var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
import { c as createLucideIcon, r as reactExports, j as jsxRuntimeExports, a as cn, _ as __vitePreload, u as ue, S as Skeleton } from "./index-CVMDOlA4.js";
import { m as Primitive, n as useControllableState, o as composeEventHandlers, p as Primitive$1, q as useComposedRefs, r as createCollection, s as useDirection, t as clamp, v as useSize, w as createContextScope$1, x as usePrevious, C as Card, a as CardHeader, b as CardTitle, g as CardDescription, d as Button, c as CardContent, h as Label, S as Select, i as SelectTrigger, j as SelectValue, k as SelectContent, l as SelectItem, B as Badge, L as LoaderCircle, y as useLayoutEffect2, z as useCallbackRef, A as useSaveVoiceSettings, D as useId, E as useGetScreenplay, F as useGetVoiceSettings, P as Play } from "./select-15-PKKK-.js";
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$h = [
  ["path", { d: "m12 19-7-7 7-7", key: "1l729n" }],
  ["path", { d: "M19 12H5", key: "x3x0zl" }]
];
const ArrowLeft = createLucideIcon("arrow-left", __iconNode$h);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$g = [
  ["circle", { cx: "12", cy: "12", r: "10", key: "1mglay" }],
  ["line", { x1: "12", x2: "12", y1: "8", y2: "12", key: "1pkeuh" }],
  ["line", { x1: "12", x2: "12.01", y1: "16", y2: "16", key: "4dfq90" }]
];
const CircleAlert = createLucideIcon("circle-alert", __iconNode$g);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$f = [
  ["circle", { cx: "12", cy: "12", r: "10", key: "1mglay" }],
  ["path", { d: "m9 12 2 2 4-4", key: "dzmm74" }]
];
const CircleCheck = createLucideIcon("circle-check", __iconNode$f);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$e = [
  ["rect", { width: "14", height: "14", x: "8", y: "8", rx: "2", ry: "2", key: "17jyea" }],
  ["path", { d: "M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2", key: "zix9uf" }]
];
const Copy = createLucideIcon("copy", __iconNode$e);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$d = [
  ["path", { d: "M12 20v2", key: "1lh1kg" }],
  ["path", { d: "M12 2v2", key: "tus03m" }],
  ["path", { d: "M17 20v2", key: "1rnc9c" }],
  ["path", { d: "M17 2v2", key: "11trls" }],
  ["path", { d: "M2 12h2", key: "1t8f8n" }],
  ["path", { d: "M2 17h2", key: "7oei6x" }],
  ["path", { d: "M2 7h2", key: "asdhe0" }],
  ["path", { d: "M20 12h2", key: "1q8mjw" }],
  ["path", { d: "M20 17h2", key: "1fpfkl" }],
  ["path", { d: "M20 7h2", key: "1o8tra" }],
  ["path", { d: "M7 20v2", key: "4gnj0m" }],
  ["path", { d: "M7 2v2", key: "1i4yhu" }],
  ["rect", { x: "4", y: "4", width: "16", height: "16", rx: "2", key: "1vbyd7" }],
  ["rect", { x: "8", y: "8", width: "8", height: "8", rx: "1", key: "z9xiuo" }]
];
const Cpu = createLucideIcon("cpu", __iconNode$d);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$c = [
  ["circle", { cx: "12", cy: "12", r: "10", key: "1mglay" }],
  ["path", { d: "M12 2a14.5 14.5 0 0 0 0 20 14.5 14.5 0 0 0 0-20", key: "13o1zl" }],
  ["path", { d: "M2 12h20", key: "9i4pu4" }]
];
const Globe = createLucideIcon("globe", __iconNode$c);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$b = [
  ["path", { d: "M12 2a3 3 0 0 0-3 3v7a3 3 0 0 0 6 0V5a3 3 0 0 0-3-3Z", key: "131961" }],
  ["path", { d: "M19 10v2a7 7 0 0 1-14 0v-2", key: "1vc78b" }],
  ["line", { x1: "12", x2: "12", y1: "19", y2: "22", key: "x3vr5v" }]
];
const Mic = createLucideIcon("mic", __iconNode$b);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$a = [
  ["rect", { x: "14", y: "4", width: "4", height: "16", rx: "1", key: "zuxfzm" }],
  ["rect", { x: "6", y: "4", width: "4", height: "16", rx: "1", key: "1okwgv" }]
];
const Pause = createLucideIcon("pause", __iconNode$a);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$9 = [
  ["path", { d: "M3 12a9 9 0 0 1 9-9 9.75 9.75 0 0 1 6.74 2.74L21 8", key: "v9h5vc" }],
  ["path", { d: "M21 3v5h-5", key: "1q7to0" }],
  ["path", { d: "M21 12a9 9 0 0 1-9 9 9.75 9.75 0 0 1-6.74-2.74L3 16", key: "3uifl3" }],
  ["path", { d: "M8 16H3v5", key: "1cv678" }]
];
const RefreshCw = createLucideIcon("refresh-cw", __iconNode$9);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$8 = [
  [
    "path",
    {
      d: "M15.2 3a2 2 0 0 1 1.4.6l3.8 3.8a2 2 0 0 1 .6 1.4V19a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2z",
      key: "1c8476"
    }
  ],
  ["path", { d: "M17 21v-7a1 1 0 0 0-1-1H8a1 1 0 0 0-1 1v7", key: "1ydtos" }],
  ["path", { d: "M7 3v4a1 1 0 0 0 1 1h7", key: "t51u73" }]
];
const Save = createLucideIcon("save", __iconNode$8);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$7 = [
  ["path", { d: "m18 14 4 4-4 4", key: "10pe0f" }],
  ["path", { d: "m18 2 4 4-4 4", key: "pucp1d" }],
  ["path", { d: "M2 18h1.973a4 4 0 0 0 3.3-1.7l5.454-8.6a4 4 0 0 1 3.3-1.7H22", key: "1ailkh" }],
  ["path", { d: "M2 6h1.972a4 4 0 0 1 3.6 2.2", key: "km57vx" }],
  ["path", { d: "M22 18h-6.041a4 4 0 0 1-3.3-1.8l-.359-.45", key: "os18l9" }]
];
const Shuffle = createLucideIcon("shuffle", __iconNode$7);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$6 = [
  ["polygon", { points: "19 20 9 12 19 4 19 20", key: "o2sva" }],
  ["line", { x1: "5", x2: "5", y1: "19", y2: "5", key: "1ocqjk" }]
];
const SkipBack = createLucideIcon("skip-back", __iconNode$6);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$5 = [
  ["polygon", { points: "5 4 15 12 5 20 5 4", key: "16p6eg" }],
  ["line", { x1: "19", x2: "19", y1: "5", y2: "19", key: "futhcm" }]
];
const SkipForward = createLucideIcon("skip-forward", __iconNode$5);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$4 = [
  [
    "path",
    {
      d: "M9.937 15.5A2 2 0 0 0 8.5 14.063l-6.135-1.582a.5.5 0 0 1 0-.962L8.5 9.936A2 2 0 0 0 9.937 8.5l1.582-6.135a.5.5 0 0 1 .963 0L14.063 8.5A2 2 0 0 0 15.5 9.937l6.135 1.581a.5.5 0 0 1 0 .964L15.5 14.063a2 2 0 0 0-1.437 1.437l-1.582 6.135a.5.5 0 0 1-.963 0z",
      key: "4pj2yx"
    }
  ],
  ["path", { d: "M20 3v4", key: "1olli1" }],
  ["path", { d: "M22 5h-4", key: "1gvqau" }],
  ["path", { d: "M4 17v2", key: "vumght" }],
  ["path", { d: "M5 18H3", key: "zchphs" }]
];
const Sparkles = createLucideIcon("sparkles", __iconNode$4);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$3 = [
  [
    "path",
    {
      d: "m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3",
      key: "wmoenq"
    }
  ],
  ["path", { d: "M12 9v4", key: "juzpu7" }],
  ["path", { d: "M12 17h.01", key: "p32p05" }]
];
const TriangleAlert = createLucideIcon("triangle-alert", __iconNode$3);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$2 = [
  [
    "path",
    {
      d: "M11 4.702a.705.705 0 0 0-1.203-.498L6.413 7.587A1.4 1.4 0 0 1 5.416 8H3a1 1 0 0 0-1 1v6a1 1 0 0 0 1 1h2.416a1.4 1.4 0 0 1 .997.413l3.383 3.384A.705.705 0 0 0 11 19.298z",
      key: "uqj9uw"
    }
  ],
  ["path", { d: "M16 9a5 5 0 0 1 0 6", key: "1q6k2b" }],
  ["path", { d: "M19.364 18.364a9 9 0 0 0 0-12.728", key: "ijwkga" }]
];
const Volume2 = createLucideIcon("volume-2", __iconNode$2);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$1 = [
  ["path", { d: "M12 20h.01", key: "zekei9" }],
  ["path", { d: "M8.5 16.429a5 5 0 0 1 7 0", key: "1bycff" }],
  ["path", { d: "M5 12.859a10 10 0 0 1 5.17-2.69", key: "1dl1wf" }],
  ["path", { d: "M19 12.859a10 10 0 0 0-2.007-1.523", key: "4k23kn" }],
  ["path", { d: "M2 8.82a15 15 0 0 1 4.177-2.643", key: "1grhjp" }],
  ["path", { d: "M22 8.82a15 15 0 0 0-11.288-3.764", key: "z3jwby" }],
  ["path", { d: "m2 2 20 20", key: "1ooewy" }]
];
const WifiOff = createLucideIcon("wifi-off", __iconNode$1);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode = [
  ["path", { d: "M18 6 6 18", key: "1bl5f8" }],
  ["path", { d: "m6 6 12 12", key: "d8bk6v" }]
];
const X = createLucideIcon("x", __iconNode);
var NAME = "Separator";
var DEFAULT_ORIENTATION = "horizontal";
var ORIENTATIONS = ["horizontal", "vertical"];
var Separator$1 = reactExports.forwardRef((props, forwardedRef) => {
  const { decorative, orientation: orientationProp = DEFAULT_ORIENTATION, ...domProps } = props;
  const orientation = isValidOrientation(orientationProp) ? orientationProp : DEFAULT_ORIENTATION;
  const ariaOrientation = orientation === "vertical" ? orientation : void 0;
  const semanticProps = decorative ? { role: "none" } : { "aria-orientation": ariaOrientation, role: "separator" };
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Primitive.div,
    {
      "data-orientation": orientation,
      ...semanticProps,
      ...domProps,
      ref: forwardedRef
    }
  );
});
Separator$1.displayName = NAME;
function isValidOrientation(orientation) {
  return ORIENTATIONS.includes(orientation);
}
var Root$4 = Separator$1;
function Separator({
  className,
  orientation = "horizontal",
  decorative = true,
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Root$4,
    {
      "data-slot": "separator",
      decorative,
      orientation,
      className: cn(
        "bg-border shrink-0 data-[orientation=horizontal]:h-px data-[orientation=horizontal]:w-full data-[orientation=vertical]:h-full data-[orientation=vertical]:w-px",
        className
      ),
      ...props
    }
  );
}
var PAGE_KEYS = ["PageUp", "PageDown"];
var ARROW_KEYS = ["ArrowUp", "ArrowDown", "ArrowLeft", "ArrowRight"];
var BACK_KEYS = {
  "from-left": ["Home", "PageDown", "ArrowDown", "ArrowLeft"],
  "from-right": ["Home", "PageDown", "ArrowDown", "ArrowRight"],
  "from-bottom": ["Home", "PageDown", "ArrowDown", "ArrowLeft"],
  "from-top": ["Home", "PageDown", "ArrowUp", "ArrowLeft"]
};
var SLIDER_NAME = "Slider";
var [Collection$1, useCollection$1, createCollectionScope$1] = createCollection(SLIDER_NAME);
var [createSliderContext] = createContextScope$1(SLIDER_NAME, [
  createCollectionScope$1
]);
var [SliderProvider, useSliderContext] = createSliderContext(SLIDER_NAME);
var Slider$1 = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      name,
      min = 0,
      max = 100,
      step = 1,
      orientation = "horizontal",
      disabled = false,
      minStepsBetweenThumbs = 0,
      defaultValue = [min],
      value,
      onValueChange = () => {
      },
      onValueCommit = () => {
      },
      inverted = false,
      form,
      ...sliderProps
    } = props;
    const thumbRefs = reactExports.useRef(/* @__PURE__ */ new Set());
    const valueIndexToChangeRef = reactExports.useRef(0);
    const isHorizontal = orientation === "horizontal";
    const SliderOrientation = isHorizontal ? SliderHorizontal : SliderVertical;
    const [values = [], setValues] = useControllableState({
      prop: value,
      defaultProp: defaultValue,
      onChange: (value2) => {
        var _a;
        const thumbs = [...thumbRefs.current];
        (_a = thumbs[valueIndexToChangeRef.current]) == null ? void 0 : _a.focus();
        onValueChange(value2);
      }
    });
    const valuesBeforeSlideStartRef = reactExports.useRef(values);
    function handleSlideStart(value2) {
      const closestIndex = getClosestValueIndex(values, value2);
      updateValues(value2, closestIndex);
    }
    function handleSlideMove(value2) {
      updateValues(value2, valueIndexToChangeRef.current);
    }
    function handleSlideEnd() {
      const prevValue = valuesBeforeSlideStartRef.current[valueIndexToChangeRef.current];
      const nextValue = values[valueIndexToChangeRef.current];
      const hasChanged = nextValue !== prevValue;
      if (hasChanged) onValueCommit(values);
    }
    function updateValues(value2, atIndex, { commit } = { commit: false }) {
      const decimalCount = getDecimalCount(step);
      const snapToStep = roundValue(Math.round((value2 - min) / step) * step + min, decimalCount);
      const nextValue = clamp(snapToStep, [min, max]);
      setValues((prevValues = []) => {
        const nextValues = getNextSortedValues(prevValues, nextValue, atIndex);
        if (hasMinStepsBetweenValues(nextValues, minStepsBetweenThumbs * step)) {
          valueIndexToChangeRef.current = nextValues.indexOf(nextValue);
          const hasChanged = String(nextValues) !== String(prevValues);
          if (hasChanged && commit) onValueCommit(nextValues);
          return hasChanged ? nextValues : prevValues;
        } else {
          return prevValues;
        }
      });
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      SliderProvider,
      {
        scope: props.__scopeSlider,
        name,
        disabled,
        min,
        max,
        valueIndexToChangeRef,
        thumbs: thumbRefs.current,
        values,
        orientation,
        form,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(Collection$1.Provider, { scope: props.__scopeSlider, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Collection$1.Slot, { scope: props.__scopeSlider, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          SliderOrientation,
          {
            "aria-disabled": disabled,
            "data-disabled": disabled ? "" : void 0,
            ...sliderProps,
            ref: forwardedRef,
            onPointerDown: composeEventHandlers(sliderProps.onPointerDown, () => {
              if (!disabled) valuesBeforeSlideStartRef.current = values;
            }),
            min,
            max,
            inverted,
            onSlideStart: disabled ? void 0 : handleSlideStart,
            onSlideMove: disabled ? void 0 : handleSlideMove,
            onSlideEnd: disabled ? void 0 : handleSlideEnd,
            onHomeKeyDown: () => !disabled && updateValues(min, 0, { commit: true }),
            onEndKeyDown: () => !disabled && updateValues(max, values.length - 1, { commit: true }),
            onStepKeyDown: ({ event, direction: stepDirection }) => {
              if (!disabled) {
                const isPageKey = PAGE_KEYS.includes(event.key);
                const isSkipKey = isPageKey || event.shiftKey && ARROW_KEYS.includes(event.key);
                const multiplier = isSkipKey ? 10 : 1;
                const atIndex = valueIndexToChangeRef.current;
                const value2 = values[atIndex];
                const stepInDirection = step * multiplier * stepDirection;
                updateValues(value2 + stepInDirection, atIndex, { commit: true });
              }
            }
          }
        ) }) })
      }
    );
  }
);
Slider$1.displayName = SLIDER_NAME;
var [SliderOrientationProvider, useSliderOrientationContext] = createSliderContext(SLIDER_NAME, {
  startEdge: "left",
  endEdge: "right",
  size: "width",
  direction: 1
});
var SliderHorizontal = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      min,
      max,
      dir,
      inverted,
      onSlideStart,
      onSlideMove,
      onSlideEnd,
      onStepKeyDown,
      ...sliderProps
    } = props;
    const [slider, setSlider] = reactExports.useState(null);
    const composedRefs = useComposedRefs(forwardedRef, (node) => setSlider(node));
    const rectRef = reactExports.useRef(void 0);
    const direction = useDirection(dir);
    const isDirectionLTR = direction === "ltr";
    const isSlidingFromLeft = isDirectionLTR && !inverted || !isDirectionLTR && inverted;
    function getValueFromPointer(pointerPosition) {
      const rect = rectRef.current || slider.getBoundingClientRect();
      const input = [0, rect.width];
      const output = isSlidingFromLeft ? [min, max] : [max, min];
      const value = linearScale$1(input, output);
      rectRef.current = rect;
      return value(pointerPosition - rect.left);
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      SliderOrientationProvider,
      {
        scope: props.__scopeSlider,
        startEdge: isSlidingFromLeft ? "left" : "right",
        endEdge: isSlidingFromLeft ? "right" : "left",
        direction: isSlidingFromLeft ? 1 : -1,
        size: "width",
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          SliderImpl,
          {
            dir: direction,
            "data-orientation": "horizontal",
            ...sliderProps,
            ref: composedRefs,
            style: {
              ...sliderProps.style,
              ["--radix-slider-thumb-transform"]: "translateX(-50%)"
            },
            onSlideStart: (event) => {
              const value = getValueFromPointer(event.clientX);
              onSlideStart == null ? void 0 : onSlideStart(value);
            },
            onSlideMove: (event) => {
              const value = getValueFromPointer(event.clientX);
              onSlideMove == null ? void 0 : onSlideMove(value);
            },
            onSlideEnd: () => {
              rectRef.current = void 0;
              onSlideEnd == null ? void 0 : onSlideEnd();
            },
            onStepKeyDown: (event) => {
              const slideDirection = isSlidingFromLeft ? "from-left" : "from-right";
              const isBackKey = BACK_KEYS[slideDirection].includes(event.key);
              onStepKeyDown == null ? void 0 : onStepKeyDown({ event, direction: isBackKey ? -1 : 1 });
            }
          }
        )
      }
    );
  }
);
var SliderVertical = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      min,
      max,
      inverted,
      onSlideStart,
      onSlideMove,
      onSlideEnd,
      onStepKeyDown,
      ...sliderProps
    } = props;
    const sliderRef = reactExports.useRef(null);
    const ref = useComposedRefs(forwardedRef, sliderRef);
    const rectRef = reactExports.useRef(void 0);
    const isSlidingFromBottom = !inverted;
    function getValueFromPointer(pointerPosition) {
      const rect = rectRef.current || sliderRef.current.getBoundingClientRect();
      const input = [0, rect.height];
      const output = isSlidingFromBottom ? [max, min] : [min, max];
      const value = linearScale$1(input, output);
      rectRef.current = rect;
      return value(pointerPosition - rect.top);
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      SliderOrientationProvider,
      {
        scope: props.__scopeSlider,
        startEdge: isSlidingFromBottom ? "bottom" : "top",
        endEdge: isSlidingFromBottom ? "top" : "bottom",
        size: "height",
        direction: isSlidingFromBottom ? 1 : -1,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          SliderImpl,
          {
            "data-orientation": "vertical",
            ...sliderProps,
            ref,
            style: {
              ...sliderProps.style,
              ["--radix-slider-thumb-transform"]: "translateY(50%)"
            },
            onSlideStart: (event) => {
              const value = getValueFromPointer(event.clientY);
              onSlideStart == null ? void 0 : onSlideStart(value);
            },
            onSlideMove: (event) => {
              const value = getValueFromPointer(event.clientY);
              onSlideMove == null ? void 0 : onSlideMove(value);
            },
            onSlideEnd: () => {
              rectRef.current = void 0;
              onSlideEnd == null ? void 0 : onSlideEnd();
            },
            onStepKeyDown: (event) => {
              const slideDirection = isSlidingFromBottom ? "from-bottom" : "from-top";
              const isBackKey = BACK_KEYS[slideDirection].includes(event.key);
              onStepKeyDown == null ? void 0 : onStepKeyDown({ event, direction: isBackKey ? -1 : 1 });
            }
          }
        )
      }
    );
  }
);
var SliderImpl = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeSlider,
      onSlideStart,
      onSlideMove,
      onSlideEnd,
      onHomeKeyDown,
      onEndKeyDown,
      onStepKeyDown,
      ...sliderProps
    } = props;
    const context = useSliderContext(SLIDER_NAME, __scopeSlider);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive$1.span,
      {
        ...sliderProps,
        ref: forwardedRef,
        onKeyDown: composeEventHandlers(props.onKeyDown, (event) => {
          if (event.key === "Home") {
            onHomeKeyDown(event);
            event.preventDefault();
          } else if (event.key === "End") {
            onEndKeyDown(event);
            event.preventDefault();
          } else if (PAGE_KEYS.concat(ARROW_KEYS).includes(event.key)) {
            onStepKeyDown(event);
            event.preventDefault();
          }
        }),
        onPointerDown: composeEventHandlers(props.onPointerDown, (event) => {
          const target = event.target;
          target.setPointerCapture(event.pointerId);
          event.preventDefault();
          if (context.thumbs.has(target)) {
            target.focus();
          } else {
            onSlideStart(event);
          }
        }),
        onPointerMove: composeEventHandlers(props.onPointerMove, (event) => {
          const target = event.target;
          if (target.hasPointerCapture(event.pointerId)) onSlideMove(event);
        }),
        onPointerUp: composeEventHandlers(props.onPointerUp, (event) => {
          const target = event.target;
          if (target.hasPointerCapture(event.pointerId)) {
            target.releasePointerCapture(event.pointerId);
            onSlideEnd(event);
          }
        })
      }
    );
  }
);
var TRACK_NAME = "SliderTrack";
var SliderTrack = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSlider, ...trackProps } = props;
    const context = useSliderContext(TRACK_NAME, __scopeSlider);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive$1.span,
      {
        "data-disabled": context.disabled ? "" : void 0,
        "data-orientation": context.orientation,
        ...trackProps,
        ref: forwardedRef
      }
    );
  }
);
SliderTrack.displayName = TRACK_NAME;
var RANGE_NAME = "SliderRange";
var SliderRange = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSlider, ...rangeProps } = props;
    const context = useSliderContext(RANGE_NAME, __scopeSlider);
    const orientation = useSliderOrientationContext(RANGE_NAME, __scopeSlider);
    const ref = reactExports.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, ref);
    const valuesCount = context.values.length;
    const percentages = context.values.map(
      (value) => convertValueToPercentage(value, context.min, context.max)
    );
    const offsetStart = valuesCount > 1 ? Math.min(...percentages) : 0;
    const offsetEnd = 100 - Math.max(...percentages);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive$1.span,
      {
        "data-orientation": context.orientation,
        "data-disabled": context.disabled ? "" : void 0,
        ...rangeProps,
        ref: composedRefs,
        style: {
          ...props.style,
          [orientation.startEdge]: offsetStart + "%",
          [orientation.endEdge]: offsetEnd + "%"
        }
      }
    );
  }
);
SliderRange.displayName = RANGE_NAME;
var THUMB_NAME$1 = "SliderThumb";
var SliderThumb = reactExports.forwardRef(
  (props, forwardedRef) => {
    const getItems = useCollection$1(props.__scopeSlider);
    const [thumb, setThumb] = reactExports.useState(null);
    const composedRefs = useComposedRefs(forwardedRef, (node) => setThumb(node));
    const index = reactExports.useMemo(
      () => thumb ? getItems().findIndex((item) => item.ref.current === thumb) : -1,
      [getItems, thumb]
    );
    return /* @__PURE__ */ jsxRuntimeExports.jsx(SliderThumbImpl, { ...props, ref: composedRefs, index });
  }
);
var SliderThumbImpl = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSlider, index, name, ...thumbProps } = props;
    const context = useSliderContext(THUMB_NAME$1, __scopeSlider);
    const orientation = useSliderOrientationContext(THUMB_NAME$1, __scopeSlider);
    const [thumb, setThumb] = reactExports.useState(null);
    const composedRefs = useComposedRefs(forwardedRef, (node) => setThumb(node));
    const isFormControl = thumb ? context.form || !!thumb.closest("form") : true;
    const size = useSize(thumb);
    const value = context.values[index];
    const percent = value === void 0 ? 0 : convertValueToPercentage(value, context.min, context.max);
    const label = getLabel(index, context.values.length);
    const orientationSize = size == null ? void 0 : size[orientation.size];
    const thumbInBoundsOffset = orientationSize ? getThumbInBoundsOffset(orientationSize, percent, orientation.direction) : 0;
    reactExports.useEffect(() => {
      if (thumb) {
        context.thumbs.add(thumb);
        return () => {
          context.thumbs.delete(thumb);
        };
      }
    }, [thumb, context.thumbs]);
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "span",
      {
        style: {
          transform: "var(--radix-slider-thumb-transform)",
          position: "absolute",
          [orientation.startEdge]: `calc(${percent}% + ${thumbInBoundsOffset}px)`
        },
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Collection$1.ItemSlot, { scope: props.__scopeSlider, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            Primitive$1.span,
            {
              role: "slider",
              "aria-label": props["aria-label"] || label,
              "aria-valuemin": context.min,
              "aria-valuenow": value,
              "aria-valuemax": context.max,
              "aria-orientation": context.orientation,
              "data-orientation": context.orientation,
              "data-disabled": context.disabled ? "" : void 0,
              tabIndex: context.disabled ? void 0 : 0,
              ...thumbProps,
              ref: composedRefs,
              style: value === void 0 ? { display: "none" } : props.style,
              onFocus: composeEventHandlers(props.onFocus, () => {
                context.valueIndexToChangeRef.current = index;
              })
            }
          ) }),
          isFormControl && /* @__PURE__ */ jsxRuntimeExports.jsx(
            SliderBubbleInput,
            {
              name: name ?? (context.name ? context.name + (context.values.length > 1 ? "[]" : "") : void 0),
              form: context.form,
              value
            },
            index
          )
        ]
      }
    );
  }
);
SliderThumb.displayName = THUMB_NAME$1;
var BUBBLE_INPUT_NAME = "RadioBubbleInput";
var SliderBubbleInput = reactExports.forwardRef(
  ({ __scopeSlider, value, ...props }, forwardedRef) => {
    const ref = reactExports.useRef(null);
    const composedRefs = useComposedRefs(ref, forwardedRef);
    const prevValue = usePrevious(value);
    reactExports.useEffect(() => {
      const input = ref.current;
      if (!input) return;
      const inputProto = window.HTMLInputElement.prototype;
      const descriptor = Object.getOwnPropertyDescriptor(inputProto, "value");
      const setValue = descriptor.set;
      if (prevValue !== value && setValue) {
        const event = new Event("input", { bubbles: true });
        setValue.call(input, value);
        input.dispatchEvent(event);
      }
    }, [prevValue, value]);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive$1.input,
      {
        style: { display: "none" },
        ...props,
        ref: composedRefs,
        defaultValue: value
      }
    );
  }
);
SliderBubbleInput.displayName = BUBBLE_INPUT_NAME;
function getNextSortedValues(prevValues = [], nextValue, atIndex) {
  const nextValues = [...prevValues];
  nextValues[atIndex] = nextValue;
  return nextValues.sort((a, b) => a - b);
}
function convertValueToPercentage(value, min, max) {
  const maxSteps = max - min;
  const percentPerStep = 100 / maxSteps;
  const percentage = percentPerStep * (value - min);
  return clamp(percentage, [0, 100]);
}
function getLabel(index, totalValues) {
  if (totalValues > 2) {
    return `Value ${index + 1} of ${totalValues}`;
  } else if (totalValues === 2) {
    return ["Minimum", "Maximum"][index];
  } else {
    return void 0;
  }
}
function getClosestValueIndex(values, nextValue) {
  if (values.length === 1) return 0;
  const distances = values.map((value) => Math.abs(value - nextValue));
  const closestDistance = Math.min(...distances);
  return distances.indexOf(closestDistance);
}
function getThumbInBoundsOffset(width, left, direction) {
  const halfWidth = width / 2;
  const halfPercent = 50;
  const offset = linearScale$1([0, halfPercent], [0, halfWidth]);
  return (halfWidth - offset(left) * direction) * direction;
}
function getStepsBetweenValues(values) {
  return values.slice(0, -1).map((value, index) => values[index + 1] - value);
}
function hasMinStepsBetweenValues(values, minStepsBetweenValues) {
  if (minStepsBetweenValues > 0) {
    const stepsBetweenValues = getStepsBetweenValues(values);
    const actualMinStepsBetweenValues = Math.min(...stepsBetweenValues);
    return actualMinStepsBetweenValues >= minStepsBetweenValues;
  }
  return true;
}
function linearScale$1(input, output) {
  return (value) => {
    if (input[0] === input[1] || output[0] === output[1]) return output[0];
    const ratio = (output[1] - output[0]) / (input[1] - input[0]);
    return output[0] + ratio * (value - input[0]);
  };
}
function getDecimalCount(value) {
  return (String(value).split(".")[1] || "").length;
}
function roundValue(value, decimalCount) {
  const rounder = Math.pow(10, decimalCount);
  return Math.round(value * rounder) / rounder;
}
var Root$3 = Slider$1;
var Track = SliderTrack;
var Range = SliderRange;
var Thumb = SliderThumb;
function Slider({
  className,
  defaultValue,
  value,
  min = 0,
  max = 100,
  ...props
}) {
  const _values = reactExports.useMemo(
    () => Array.isArray(value) ? value : Array.isArray(defaultValue) ? defaultValue : [min, max],
    [value, defaultValue, min, max]
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    Root$3,
    {
      "data-slot": "slider",
      defaultValue,
      value,
      min,
      max,
      className: cn(
        "relative flex w-full touch-none items-center select-none data-[disabled]:opacity-50 data-[orientation=vertical]:h-full data-[orientation=vertical]:min-h-44 data-[orientation=vertical]:w-auto data-[orientation=vertical]:flex-col",
        className
      ),
      ...props,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Track,
          {
            "data-slot": "slider-track",
            className: cn(
              "bg-muted relative grow overflow-hidden rounded-full data-[orientation=horizontal]:h-1.5 data-[orientation=horizontal]:w-full data-[orientation=vertical]:h-full data-[orientation=vertical]:w-1.5"
            ),
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              Range,
              {
                "data-slot": "slider-range",
                className: cn(
                  "bg-primary absolute data-[orientation=horizontal]:h-full data-[orientation=vertical]:w-full"
                )
              }
            )
          }
        ),
        Array.from({ length: _values.length }, (value2, _) => /* @__PURE__ */ jsxRuntimeExports.jsx(
          Thumb,
          {
            "data-slot": "slider-thumb",
            className: "border-primary bg-background ring-ring/50 block size-4 shrink-0 rounded-full border shadow-sm transition-[color,box-shadow] hover:ring-4 focus-visible:ring-4 focus-visible:outline-hidden disabled:pointer-events-none disabled:opacity-50"
          },
          `${value2}`
        ))
      ]
    }
  );
}
const STORAGE_KEY = "tts_active_provider";
class TTSRegistry {
  constructor() {
    __publicField(this, "providers", /* @__PURE__ */ new Map());
    __publicField(this, "activeId", null);
    __publicField(this, "listeners", /* @__PURE__ */ new Set());
  }
  /** Register a provider. First registered becomes the default. */
  registerProvider(provider) {
    this.providers.set(provider.id, provider);
    if (this.activeId === null) {
      const stored = this.readStorage();
      this.activeId = stored ?? provider.id;
    }
    this.notify();
  }
  getProvider(id) {
    return this.providers.get(id);
  }
  listProviders() {
    return Array.from(this.providers.values());
  }
  getActiveProvider() {
    if (this.activeId && this.providers.has(this.activeId)) {
      return this.providers.get(this.activeId);
    }
    return this.providers.values().next().value;
  }
  setActiveProvider(id) {
    if (!this.providers.has(id)) return;
    this.activeId = id;
    this.writeStorage(id);
    this.notify();
  }
  getActiveId() {
    return this.activeId;
  }
  /** Subscribe to registry changes (provider switch, status update) */
  addListener(listener) {
    this.listeners.add(listener);
  }
  removeListener(listener) {
    this.listeners.delete(listener);
  }
  notify() {
    for (const l of this.listeners) l();
  }
  readStorage() {
    try {
      return localStorage.getItem(STORAGE_KEY);
    } catch {
      return null;
    }
  }
  writeStorage(id) {
    try {
      localStorage.setItem(STORAGE_KEY, id);
    } catch {
    }
  }
}
const ttsRegistry = new TTSRegistry();
const KOKORO_VOICES = [
  { id: "af_heart", name: "Heart (F)", lang: "en-US" },
  { id: "af_bella", name: "Bella (F)", lang: "en-US" },
  { id: "af_nicole", name: "Nicole (F)", lang: "en-US" },
  { id: "af_aoede", name: "Aoede (F)", lang: "en-US" },
  { id: "af_kore", name: "Kore (F)", lang: "en-US" },
  { id: "af_sarah", name: "Sarah (F)", lang: "en-US" },
  { id: "af_nova", name: "Nova (F)", lang: "en-US" },
  { id: "af_sky", name: "Sky (F)", lang: "en-US" },
  { id: "af_alloy", name: "Alloy (F)", lang: "en-US" },
  { id: "af_jessica", name: "Jessica (F)", lang: "en-US" },
  { id: "af_river", name: "River (F)", lang: "en-US" },
  { id: "am_adam", name: "Adam (M)", lang: "en-US" },
  { id: "am_michael", name: "Michael (M)", lang: "en-US" },
  { id: "am_fenrir", name: "Fenrir (M)", lang: "en-US" },
  { id: "am_puck", name: "Puck (M)", lang: "en-US" },
  { id: "am_echo", name: "Echo (M)", lang: "en-US" },
  { id: "am_eric", name: "Eric (M)", lang: "en-US" },
  { id: "am_liam", name: "Liam (M)", lang: "en-US" },
  { id: "am_onyx", name: "Onyx (M)", lang: "en-US" },
  { id: "am_santa", name: "Santa (M)", lang: "en-US" },
  { id: "am_zeus", name: "Zeus (M)", lang: "en-US" },
  { id: "bf_emma", name: "Emma / British (F)", lang: "en-GB" },
  { id: "bf_isabella", name: "Isabella / British (F)", lang: "en-GB" },
  { id: "bf_alice", name: "Alice / British (F)", lang: "en-GB" },
  { id: "bf_lily", name: "Lily / British (F)", lang: "en-GB" },
  { id: "bm_george", name: "George / British (M)", lang: "en-GB" },
  { id: "bm_fable", name: "Fable / British (M)", lang: "en-GB" },
  { id: "bm_lewis", name: "Lewis / British (M)", lang: "en-GB" },
  { id: "bm_daniel", name: "Daniel / British (M)", lang: "en-GB" }
];
const KOKORO_MODEL_ID = "onnx-community/Kokoro-82M-v1.0-ONNX";
class KokoroProvider {
  constructor() {
    __publicField(this, "id", "kokoro");
    __publicField(this, "name", "Kokoro TTS");
    __publicField(this, "description", "High-quality neural voices (82M params, runs locally)");
    __publicField(this, "status", "unavailable");
    __publicField(this, "loadingProgress", 0);
    __publicField(this, "loadError", null);
    __publicField(this, "listeners", /* @__PURE__ */ new Set());
    __publicField(this, "tts", null);
    __publicField(this, "loadPromise", null);
    __publicField(this, "currentAudio", null);
  }
  isAvailable() {
    return typeof window !== "undefined" && typeof WebAssembly !== "undefined" && typeof window.AudioContext !== "undefined";
  }
  isReady() {
    return this.status === "ready";
  }
  getStatus() {
    return this.status;
  }
  getLoadingProgress() {
    return this.loadingProgress;
  }
  /** Returns the last error message if status === 'error', otherwise null */
  getLoadError() {
    return this.loadError;
  }
  getVoices() {
    return KOKORO_VOICES;
  }
  addListener(listener) {
    this.listeners.add(listener);
  }
  removeListener(listener) {
    this.listeners.delete(listener);
  }
  notify() {
    for (const l of this.listeners) l();
  }
  setProgress(p) {
    this.loadingProgress = p;
    this.notify();
  }
  async loadModel() {
    if (this.tts !== null) return;
    if (this.loadPromise) return this.loadPromise;
    this.status = "loading";
    this.loadingProgress = 0;
    this.loadError = null;
    this.notify();
    console.info("[KokoroProvider] Starting initialization...");
    this.loadPromise = (async () => {
      try {
        const { KokoroTTS } = await __vitePreload(async () => {
          const { KokoroTTS: KokoroTTS2 } = await import("./kokoro-HJQBcFCA.js");
          return { KokoroTTS: KokoroTTS2 };
        }, true ? [] : void 0);
        const tts = await KokoroTTS.from_pretrained(KOKORO_MODEL_ID, {
          dtype: "q8",
          device: "wasm",
          progress_callback: (info) => {
            if (info.status === "progress" && typeof info.progress === "number") {
              const pct = Math.round(info.progress);
              console.info(`[KokoroProvider] Downloading model... ${pct}%`);
              this.setProgress(pct);
            } else if (info.status === "initiate") {
              console.info("[KokoroProvider] Model download initiated");
              this.setProgress(5);
            } else if (info.status === "download") {
              this.setProgress(Math.max(this.loadingProgress, 10));
            } else if (info.status === "done") {
              console.info(
                "[KokoroProvider] Model files downloaded, initializing WASM..."
              );
              this.setProgress(90);
            }
          }
        });
        this.tts = tts;
        this.loadingProgress = 100;
        this.loadError = null;
        this.status = "ready";
        this.notify();
        console.info("[KokoroProvider] Ready ✓ — all 29 voices available");
      } catch (err) {
        const errorMessage = err instanceof Error ? err.message : typeof err === "string" ? err : "Unknown error";
        console.error(
          "[KokoroProvider] FAILED to load model:",
          errorMessage,
          err
        );
        this.status = "error";
        this.loadingProgress = 0;
        this.loadError = errorMessage;
        this.loadPromise = null;
        this.notify();
        throw err;
      }
    })();
    return this.loadPromise;
  }
  /**
   * Proactively initialize the model. Call this on app mount so the model
   * downloads in the background before the user hits play.
   */
  async initialize() {
    if (!this.isAvailable()) {
      console.warn(
        "[KokoroProvider] WebAssembly or AudioContext not available — skipping init"
      );
      return;
    }
    this.loadModel().catch(() => {
    });
  }
  /**
   * Retry a failed initialization. Resets error state and tries again.
   */
  async retry() {
    if (this.status !== "error") return;
    this.tts = null;
    this.loadPromise = null;
    this.loadError = null;
    console.info("[KokoroProvider] Retrying initialization...");
    await this.initialize();
  }
  async synthesize(text, settings) {
    if (!this.isAvailable()) {
      throw new Error("Kokoro TTS is not available in this environment");
    }
    await this.loadModel();
    if (this.status !== "ready" || !this.tts) {
      throw new Error("Kokoro model is not ready");
    }
    const voiceId = settings.voiceId && KOKORO_VOICES.find((v) => v.id === settings.voiceId) ? settings.voiceId : "af_heart";
    const speed = settings.rate ?? 1;
    const audio = await this.tts.generate(text, { voice: voiceId, speed });
    const blob = await audio.toBlob();
    const url = URL.createObjectURL(blob);
    return new Promise((resolve, reject) => {
      const el = new Audio(url);
      this.currentAudio = el;
      el.onended = () => {
        this.currentAudio = null;
        URL.revokeObjectURL(url);
        resolve();
      };
      el.onerror = () => {
        this.currentAudio = null;
        URL.revokeObjectURL(url);
        reject(new Error("Audio playback error"));
      };
      el.play().catch((err) => {
        this.currentAudio = null;
        URL.revokeObjectURL(url);
        reject(err);
      });
    });
  }
  cancel() {
    if (this.currentAudio) {
      this.currentAudio.pause();
      this.currentAudio.src = "";
      this.currentAudio = null;
    }
  }
  dispose() {
    this.cancel();
    this.tts = null;
    this.loadPromise = null;
    this.status = "unavailable";
    this.loadError = null;
    this.listeners.clear();
  }
}
class WebSpeechProvider {
  constructor() {
    __publicField(this, "id", "web-speech");
    __publicField(this, "name", "Web Speech API");
    __publicField(this, "description", "Built-in browser voices — no download required");
    __publicField(this, "voices", []);
    __publicField(this, "synth", null);
    __publicField(this, "ready", false);
  }
  isAvailable() {
    return typeof window !== "undefined" && "speechSynthesis" in window;
  }
  isReady() {
    return this.ready && this.voices.length > 0;
  }
  getStatus() {
    if (!this.isAvailable()) return "unavailable";
    if (!this.ready) return "loading";
    return "ready";
  }
  getVoices() {
    if (!this.isAvailable()) return [];
    this.ensureInitialized();
    return this.voices;
  }
  /**
   * Load voices from speechSynthesis and keep them updated.
   * Call this early (e.g. on app mount) to get voices fast.
   */
  ensureInitialized(language) {
    if (!this.isAvailable()) return;
    if (this.synth) return;
    this.synth = window.speechSynthesis;
    const loadVoices = () => {
      const all = this.synth.getVoices();
      const langCode = language == null ? void 0 : language.split("-")[0].toLowerCase();
      const filtered = langCode ? all.filter((v) => v.lang.toLowerCase().startsWith(langCode)) : all;
      const source = filtered.length > 0 ? filtered : all;
      this.voices = source.map((v) => ({
        id: v.name,
        name: v.name,
        lang: v.lang,
        raw: v
      }));
      this.ready = this.voices.length > 0;
    };
    loadVoices();
    if (this.synth.onvoiceschanged !== void 0) {
      this.synth.onvoiceschanged = loadVoices;
    }
  }
  synthesize(text, settings) {
    return new Promise((resolve, reject) => {
      if (!this.isAvailable() || !this.synth) {
        reject(new Error("Web Speech API is not available"));
        return;
      }
      const utt = new SpeechSynthesisUtterance(text);
      utt.pitch = settings.pitch ?? 1;
      utt.rate = settings.rate ?? 1;
      utt.volume = 1;
      if (settings.language) utt.lang = settings.language;
      if (settings.voiceId) {
        const voice = this.synth.getVoices().find((v) => v.name === settings.voiceId);
        if (voice) utt.voice = voice;
      }
      utt.onend = () => resolve();
      utt.onerror = (e) => {
        if (e.error === "interrupted") {
          resolve();
        } else {
          reject(new Error(`Speech error: ${e.error}`));
        }
      };
      this.synth.speak(utt);
    });
  }
  cancel() {
    var _a;
    (_a = this.synth) == null ? void 0 : _a.cancel();
  }
  dispose() {
    var _a;
    (_a = this.synth) == null ? void 0 : _a.cancel();
    this.synth = null;
    this.voices = [];
    this.ready = false;
  }
}
const kokoro = new KokoroProvider();
const webSpeech = new WebSpeechProvider();
ttsRegistry.registerProvider(kokoro);
ttsRegistry.registerProvider(webSpeech);
webSpeech.ensureInitialized();
kokoro.initialize().catch(() => {
});
const CHARACTER_COLORS = {
  0: {
    bg: "bg-[oklch(0.65_0.26_35/0.2)]",
    text: "text-[oklch(0.75_0.26_35)]",
    border: "border-[oklch(0.65_0.26_35/0.4)]",
    label: "warm"
  },
  1: {
    bg: "bg-[oklch(0.70_0.24_200/0.2)]",
    text: "text-[oklch(0.80_0.24_200)]",
    border: "border-[oklch(0.70_0.24_200/0.4)]",
    label: "cool"
  },
  2: {
    bg: "bg-[oklch(0.60_0.30_340/0.2)]",
    text: "text-[oklch(0.72_0.30_340)]",
    border: "border-[oklch(0.60_0.30_340/0.4)]",
    label: "electric"
  },
  3: {
    bg: "bg-[oklch(0.70_0.26_145/0.2)]",
    text: "text-[oklch(0.78_0.26_145)]",
    border: "border-[oklch(0.70_0.26_145/0.4)]",
    label: "emerald"
  },
  4: {
    bg: "bg-[oklch(0.72_0.28_85/0.2)]",
    text: "text-[oklch(0.80_0.28_85)]",
    border: "border-[oklch(0.72_0.28_85/0.4)]",
    label: "gold"
  }
};
function getCharacterColor(index) {
  return index % 5;
}
function getInitials(name) {
  return name.split(" ").map((w) => w[0]).join("").slice(0, 2).toUpperCase();
}
function CharacterVoiceSettings({
  characterName,
  dialogueCount,
  colorIndex,
  assignment,
  voices,
  availableLanguages,
  globalLanguage,
  activeProvider,
  copiedSettings,
  onUpdate,
  onCopy,
  onPaste,
  webSpeechVoices
}) {
  var _a;
  const color = CHARACTER_COLORS[colorIndex];
  const effectiveLang = assignment.language || globalLanguage;
  const hasLanguageOverride = !!assignment.language && assignment.language !== globalLanguage;
  const filteredVoices = reactExports.useMemo(() => {
    if (voices.length === 0) return voices;
    const filtered = voices.filter((v) => {
      if (activeProvider === "kokoro") {
        const prefix = v.id.slice(0, 3);
        const voiceLang = prefix === "af_" || prefix === "am_" ? "en-US" : prefix === "bf_" || prefix === "bm_" ? "en-GB" : v.lang;
        return voiceLang === effectiveLang;
      }
      const langPrefix = effectiveLang.split("-")[0];
      return v.lang === effectiveLang || v.lang.startsWith(`${langPrefix}-`) || v.lang === langPrefix;
    });
    return filtered.length > 0 ? filtered : voices;
  }, [voices, effectiveLang, activeProvider]);
  const voicesFiltered = filteredVoices.length < voices.length;
  const testVoice = async () => {
    const provider = ttsRegistry.getActiveProvider();
    if (!(provider == null ? void 0 : provider.isReady())) {
      const utt = new SpeechSynthesisUtterance(`Hello, I am ${characterName}.`);
      utt.pitch = assignment.pitch;
      utt.rate = assignment.rate;
      if (assignment.language) utt.lang = assignment.language;
      if (assignment.voiceName) {
        const v = webSpeechVoices.find(
          (vv) => vv.name === assignment.voiceName
        );
        if (v) utt.voice = v;
      }
      window.speechSynthesis.cancel();
      window.speechSynthesis.speak(utt);
      return;
    }
    provider.cancel();
    try {
      await provider.synthesize(`Hello, I am ${characterName}.`, {
        voiceId: assignment.voiceName,
        pitch: assignment.pitch,
        rate: assignment.rate,
        language: assignment.language
      });
    } catch {
      ue.error("Voice test failed");
    }
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Card, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(CardHeader, { className: "pb-3", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between gap-2 flex-wrap", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "span",
          {
            className: `character-badge w-10 h-10 text-sm ${color.bg} ${color.text} border ${color.border}`,
            children: getInitials(characterName)
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(CardTitle, { className: "font-display text-lg", children: characterName }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(CardDescription, { children: characterName === "Narrator" ? "Narration voice" : `${dialogueCount} dialogue lines` })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-2 flex-shrink-0", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          Button,
          {
            variant: "outline",
            size: "sm",
            onClick: () => {
              onCopy(assignment);
              ue.success(`Copied ${characterName}'s settings`);
            },
            disabled: !assignment.voiceName,
            "aria-label": "Copy voice settings",
            "data-ocid": "copy-settings",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Copy, { className: "w-3.5 h-3.5 mr-1" }),
              "Copy"
            ]
          }
        ),
        copiedSettings && /* @__PURE__ */ jsxRuntimeExports.jsx(
          Button,
          {
            variant: "outline",
            size: "sm",
            onClick: () => {
              onPaste();
              ue.success(`Pasted to ${characterName}`);
            },
            "data-ocid": "paste-settings",
            children: "Paste"
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          Button,
          {
            size: "sm",
            onClick: testVoice,
            className: "gap-1.5",
            "data-ocid": "test-voice",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Volume2, { className: "w-3.5 h-3.5" }),
              "Test"
            ]
          }
        )
      ] })
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(CardContent, { className: "space-y-5", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-1.5", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { htmlFor: `voice-${characterName}`, children: "Voice" }),
          voicesFiltered && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-xs text-muted-foreground", children: [
            "Filtered to ",
            effectiveLang
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          Select,
          {
            value: assignment.voiceName ?? ((_a = filteredVoices[0]) == null ? void 0 : _a.id) ?? "__no_voice__",
            onValueChange: (v) => onUpdate({ voiceName: v }),
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                SelectTrigger,
                {
                  id: `voice-${characterName}`,
                  "data-ocid": "voice-select",
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(SelectValue, { placeholder: "Select a voice" })
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(SelectContent, { children: [
                filteredVoices.map((v) => /* @__PURE__ */ jsxRuntimeExports.jsxs(SelectItem, { value: v.id, children: [
                  v.name,
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-muted-foreground text-xs ml-1", children: [
                    "(",
                    v.lang,
                    ")"
                  ] })
                ] }, v.id)),
                !voicesFiltered && voices.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "px-2 py-1.5 text-xs text-muted-foreground border-t border-border", children: [
                  "No voices match ",
                  effectiveLang,
                  " — showing all"
                ] })
              ] })
            ]
          }
        )
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Separator, {}),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-3", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { children: "Pitch" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { variant: "secondary", className: "font-mono text-xs", children: assignment.pitch.toFixed(1) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Slider,
          {
            min: 0.5,
            max: 2,
            step: 0.05,
            value: [assignment.pitch],
            onValueChange: ([v]) => onUpdate({ pitch: v }),
            className: "cursor-pointer",
            "data-ocid": "pitch-slider"
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-xs text-muted-foreground", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Low" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Normal" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "High" })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-3", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { children: "Speed" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Badge, { variant: "secondary", className: "font-mono text-xs", children: [
            assignment.rate.toFixed(1),
            "×"
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Slider,
          {
            min: 0.5,
            max: 2,
            step: 0.05,
            value: [assignment.rate],
            onValueChange: ([v]) => onUpdate({ rate: v }),
            className: "cursor-pointer",
            "data-ocid": "rate-slider"
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-xs text-muted-foreground", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Slow" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Normal" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Fast" })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Separator, {}),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-1.5", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { htmlFor: `lang-${characterName}`, children: "Language Override" }),
          hasLanguageOverride && /* @__PURE__ */ jsxRuntimeExports.jsxs(
            Button,
            {
              variant: "ghost",
              size: "sm",
              onClick: () => onUpdate({ language: void 0 }),
              className: "h-6 px-2 text-xs text-muted-foreground hover:text-foreground gap-1",
              "aria-label": "Clear language override",
              "data-ocid": "clear-language-override",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "w-3 h-3" }),
                "Reset to global"
              ]
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          Select,
          {
            value: assignment.language ?? "__global__",
            onValueChange: (v) => onUpdate({ language: v === "__global__" ? void 0 : v }),
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                SelectTrigger,
                {
                  id: `lang-${characterName}`,
                  "data-ocid": "char-language-select",
                  className: hasLanguageOverride ? "border-primary/50" : "",
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(SelectValue, { placeholder: `Global (${globalLanguage})` })
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(SelectContent, { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs(SelectItem, { value: "__global__", className: "text-muted-foreground", children: [
                  "Global (",
                  globalLanguage,
                  ")"
                ] }),
                availableLanguages.filter((lang) => lang !== globalLanguage).map((lang) => /* @__PURE__ */ jsxRuntimeExports.jsx(SelectItem, { value: lang, children: lang }, lang))
              ] })
            ]
          }
        ),
        hasLanguageOverride && /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-xs text-primary/80", children: [
          "Override: ",
          assignment.language,
          " (global: ",
          globalLanguage,
          ")"
        ] })
      ] })
    ] })
  ] });
}
function createContextScope(scopeName, createContextScopeDeps = []) {
  let defaultContexts = [];
  function createContext3(rootComponentName, defaultContext) {
    const BaseContext = reactExports.createContext(defaultContext);
    BaseContext.displayName = rootComponentName + "Context";
    const index = defaultContexts.length;
    defaultContexts = [...defaultContexts, defaultContext];
    const Provider = (props) => {
      var _a;
      const { scope, children, ...context } = props;
      const Context = ((_a = scope == null ? void 0 : scope[scopeName]) == null ? void 0 : _a[index]) || BaseContext;
      const value = reactExports.useMemo(() => context, Object.values(context));
      return /* @__PURE__ */ jsxRuntimeExports.jsx(Context.Provider, { value, children });
    };
    Provider.displayName = rootComponentName + "Provider";
    function useContext2(consumerName, scope) {
      var _a;
      const Context = ((_a = scope == null ? void 0 : scope[scopeName]) == null ? void 0 : _a[index]) || BaseContext;
      const context = reactExports.useContext(Context);
      if (context) return context;
      if (defaultContext !== void 0) return defaultContext;
      throw new Error(`\`${consumerName}\` must be used within \`${rootComponentName}\``);
    }
    return [Provider, useContext2];
  }
  const createScope = () => {
    const scopeContexts = defaultContexts.map((defaultContext) => {
      return reactExports.createContext(defaultContext);
    });
    return function useScope(scope) {
      const contexts = (scope == null ? void 0 : scope[scopeName]) || scopeContexts;
      return reactExports.useMemo(
        () => ({ [`__scope${scopeName}`]: { ...scope, [scopeName]: contexts } }),
        [scope, contexts]
      );
    };
  };
  createScope.scopeName = scopeName;
  return [createContext3, composeContextScopes(createScope, ...createContextScopeDeps)];
}
function composeContextScopes(...scopes) {
  const baseScope = scopes[0];
  if (scopes.length === 1) return baseScope;
  const createScope = () => {
    const scopeHooks = scopes.map((createScope2) => ({
      useScope: createScope2(),
      scopeName: createScope2.scopeName
    }));
    return function useComposedScopes(overrideScopes) {
      const nextScopes = scopeHooks.reduce((nextScopes2, { useScope, scopeName }) => {
        const scopeProps = useScope(overrideScopes);
        const currentScope = scopeProps[`__scope${scopeName}`];
        return { ...nextScopes2, ...currentScope };
      }, {});
      return reactExports.useMemo(() => ({ [`__scope${baseScope.scopeName}`]: nextScopes }), [nextScopes]);
    };
  };
  createScope.scopeName = baseScope.scopeName;
  return createScope;
}
var PROGRESS_NAME = "Progress";
var DEFAULT_MAX = 100;
var [createProgressContext] = createContextScope(PROGRESS_NAME);
var [ProgressProvider, useProgressContext] = createProgressContext(PROGRESS_NAME);
var Progress$1 = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeProgress,
      value: valueProp = null,
      max: maxProp,
      getValueLabel = defaultGetValueLabel,
      ...progressProps
    } = props;
    if ((maxProp || maxProp === 0) && !isValidMaxNumber(maxProp)) {
      console.error(getInvalidMaxError(`${maxProp}`, "Progress"));
    }
    const max = isValidMaxNumber(maxProp) ? maxProp : DEFAULT_MAX;
    if (valueProp !== null && !isValidValueNumber(valueProp, max)) {
      console.error(getInvalidValueError(`${valueProp}`, "Progress"));
    }
    const value = isValidValueNumber(valueProp, max) ? valueProp : null;
    const valueLabel = isNumber(value) ? getValueLabel(value, max) : void 0;
    return /* @__PURE__ */ jsxRuntimeExports.jsx(ProgressProvider, { scope: __scopeProgress, value, max, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.div,
      {
        "aria-valuemax": max,
        "aria-valuemin": 0,
        "aria-valuenow": isNumber(value) ? value : void 0,
        "aria-valuetext": valueLabel,
        role: "progressbar",
        "data-state": getProgressState(value, max),
        "data-value": value ?? void 0,
        "data-max": max,
        ...progressProps,
        ref: forwardedRef
      }
    ) });
  }
);
Progress$1.displayName = PROGRESS_NAME;
var INDICATOR_NAME = "ProgressIndicator";
var ProgressIndicator = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeProgress, ...indicatorProps } = props;
    const context = useProgressContext(INDICATOR_NAME, __scopeProgress);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.div,
      {
        "data-state": getProgressState(context.value, context.max),
        "data-value": context.value ?? void 0,
        "data-max": context.max,
        ...indicatorProps,
        ref: forwardedRef
      }
    );
  }
);
ProgressIndicator.displayName = INDICATOR_NAME;
function defaultGetValueLabel(value, max) {
  return `${Math.round(value / max * 100)}%`;
}
function getProgressState(value, maxValue) {
  return value == null ? "indeterminate" : value === maxValue ? "complete" : "loading";
}
function isNumber(value) {
  return typeof value === "number";
}
function isValidMaxNumber(max) {
  return isNumber(max) && !isNaN(max) && max > 0;
}
function isValidValueNumber(value, max) {
  return isNumber(value) && !isNaN(value) && value <= max && value >= 0;
}
function getInvalidMaxError(propValue, componentName) {
  return `Invalid prop \`max\` of value \`${propValue}\` supplied to \`${componentName}\`. Only numbers greater than 0 are valid max values. Defaulting to \`${DEFAULT_MAX}\`.`;
}
function getInvalidValueError(propValue, componentName) {
  return `Invalid prop \`value\` of value \`${propValue}\` supplied to \`${componentName}\`. The \`value\` prop must be:
  - a positive number
  - less than the value passed to \`max\` (or ${DEFAULT_MAX} if no \`max\` prop is set)
  - \`null\` or \`undefined\` if the progress is indeterminate.

Defaulting to \`null\`.`;
}
var Root$2 = Progress$1;
var Indicator = ProgressIndicator;
function Progress({
  className,
  value,
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Root$2,
    {
      "data-slot": "progress",
      className: cn(
        "bg-primary/20 relative h-2 w-full overflow-hidden rounded-full",
        className
      ),
      ...props,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        Indicator,
        {
          "data-slot": "progress-indicator",
          className: "bg-primary h-full w-full flex-1 transition-all",
          style: { transform: `translateX(-${100 - (value || 0)}%)` }
        }
      )
    }
  );
}
function getSnapshot() {
  const parts = [ttsRegistry.getActiveId() ?? "none"];
  for (const p of ttsRegistry.listProviders()) {
    const progress = "getLoadingProgress" in p ? p.getLoadingProgress() : 0;
    const error = "getLoadError" in p ? p.getLoadError() ?? "" : "";
    parts.push(`${p.id}:${p.getStatus()}:${progress}:${error}`);
  }
  return parts.join("|");
}
function subscribe(callback) {
  ttsRegistry.addListener(callback);
  for (const p of ttsRegistry.listProviders()) {
    if ("addListener" in p) {
      p.addListener(callback);
    }
  }
  return () => {
    ttsRegistry.removeListener(callback);
    for (const p of ttsRegistry.listProviders()) {
      if ("removeListener" in p) {
        p.removeListener(callback);
      }
    }
  };
}
function buildProviderInfo(id) {
  const p = ttsRegistry.getProvider(id);
  if (!p) return null;
  const progress = "getLoadingProgress" in p ? p.getLoadingProgress() : 0;
  const loadError = "getLoadError" in p ? p.getLoadError() : null;
  return {
    id: p.id,
    name: p.name,
    description: p.description,
    status: p.getStatus(),
    loadingProgress: progress,
    loadError,
    voices: p.getVoices()
  };
}
function useTTSProvider() {
  var _a;
  reactExports.useSyncExternalStore(subscribe, getSnapshot, getSnapshot);
  const activeProvider = ttsRegistry.getActiveProvider();
  const allProviders = ttsRegistry.listProviders();
  const providers = allProviders.map((p) => buildProviderInfo(p.id)).filter((p) => p !== null);
  const activeInfo = activeProvider ? buildProviderInfo(activeProvider.id) : null;
  const setProvider = reactExports.useCallback((id) => {
    ttsRegistry.setActiveProvider(id);
  }, []);
  reactExports.useEffect(() => {
    const webSpeech2 = ttsRegistry.getProvider("web-speech");
    if (webSpeech2 && "ensureInitialized" in webSpeech2) {
      webSpeech2.ensureInitialized();
    }
  }, []);
  const kokoroProvider = ttsRegistry.getProvider("kokoro");
  const isKokoroLoading = (kokoroProvider == null ? void 0 : kokoroProvider.getStatus()) === "loading";
  const kokoroProgress = (kokoroProvider == null ? void 0 : kokoroProvider.getLoadingProgress()) ?? 0;
  const kokoroError = ((_a = kokoroProvider == null ? void 0 : kokoroProvider.getLoadError) == null ? void 0 : _a.call(kokoroProvider)) ?? null;
  const retryKokoro = reactExports.useCallback(() => {
    const kp = ttsRegistry.getProvider("kokoro");
    kp == null ? void 0 : kp.retry().catch(() => {
    });
  }, []);
  return {
    activeProvider: activeInfo,
    providers,
    setProvider,
    isKokoroLoading,
    kokoroProgress,
    kokoroError,
    retryKokoro
  };
}
function StatusBadge({
  status,
  progress
}) {
  switch (status) {
    case "ready":
      return /* @__PURE__ */ jsxRuntimeExports.jsxs(
        Badge,
        {
          variant: "secondary",
          className: "bg-success/10 text-success border-success/20 gap-1 text-xs",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheck, { className: "w-3 h-3" }),
            "Ready"
          ]
        }
      );
    case "loading":
      return /* @__PURE__ */ jsxRuntimeExports.jsxs(
        Badge,
        {
          variant: "secondary",
          className: "bg-primary/10 text-primary border-primary/20 gap-1 text-xs",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "w-3 h-3 animate-spin" }),
            "Loading ",
            progress,
            "%"
          ]
        }
      );
    case "error":
      return /* @__PURE__ */ jsxRuntimeExports.jsxs(
        Badge,
        {
          variant: "secondary",
          className: "bg-destructive/10 text-destructive border-destructive/20 gap-1 text-xs",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "w-3 h-3" }),
            "Failed"
          ]
        }
      );
    case "unavailable":
      return /* @__PURE__ */ jsxRuntimeExports.jsxs(
        Badge,
        {
          variant: "secondary",
          className: "bg-muted text-muted-foreground text-xs gap-1",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(WifiOff, { className: "w-3 h-3" }),
            "Unavailable"
          ]
        }
      );
  }
}
function ProviderIcon({ id }) {
  if (id === "kokoro") return /* @__PURE__ */ jsxRuntimeExports.jsx(Cpu, { className: "w-4 h-4" });
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Mic, { className: "w-4 h-4" });
}
function TTSProviderSelector() {
  const {
    providers,
    activeProvider,
    setProvider,
    isKokoroLoading,
    kokoroProgress,
    kokoroError,
    retryKokoro
  } = useTTSProvider();
  if (providers.length === 0) return null;
  const kokoroProvider = providers.find((p) => p.id === "kokoro");
  const kokoroIsActive = (activeProvider == null ? void 0 : activeProvider.id) === "kokoro";
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-3", "data-ocid": "tts-provider-selector", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "grid gap-2 sm:grid-cols-2", children: providers.map((p) => {
      const isActive = (activeProvider == null ? void 0 : activeProvider.id) === p.id;
      const isUnavailable = p.status === "unavailable";
      return /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "button",
        {
          type: "button",
          disabled: isUnavailable,
          onClick: () => !isUnavailable && setProvider(p.id),
          className: `
                flex items-start gap-3 rounded-lg border px-4 py-3 text-left transition-all
                ${isActive ? "border-primary bg-primary/10 ring-1 ring-primary/40 shadow-sm" : isUnavailable ? "border-border/40 bg-muted/30 opacity-50 cursor-not-allowed" : "border-border bg-card hover:border-primary/40 hover:bg-primary/5 cursor-pointer"}
              `,
          "data-ocid": `provider-${p.id}`,
          "aria-pressed": isActive,
          "aria-label": `Use ${p.name}`,
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "span",
              {
                className: `mt-0.5 flex-shrink-0 ${isActive ? "text-primary" : "text-muted-foreground"}`,
                children: /* @__PURE__ */ jsxRuntimeExports.jsx(ProviderIcon, { id: p.id })
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 min-w-0 space-y-1", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 flex-wrap", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "span",
                  {
                    className: `text-sm font-semibold ${isActive ? "text-primary" : "text-foreground"}`,
                    children: p.name
                  }
                ),
                isActive && /* @__PURE__ */ jsxRuntimeExports.jsx(
                  Badge,
                  {
                    variant: "secondary",
                    className: "text-[10px] px-1.5 py-0 h-4 bg-primary/20 text-primary border-primary/30",
                    children: "Active"
                  }
                )
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground leading-snug", children: p.description }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "pt-0.5", children: /* @__PURE__ */ jsxRuntimeExports.jsx(StatusBadge, { status: p.status, progress: p.loadingProgress }) })
            ] })
          ]
        },
        p.id
      );
    }) }),
    isKokoroLoading && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: "rounded-lg border border-primary/20 bg-primary/5 px-4 py-3 space-y-2",
        "data-ocid": "kokoro-loading-banner",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 text-sm text-primary", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "w-4 h-4 animate-spin flex-shrink-0" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "font-medium", children: [
              "Loading Kokoro voice model (",
              kokoroProgress,
              "%)…"
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Progress, { value: kokoroProgress, className: "h-1.5 bg-primary/10" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: "High-quality neural voices are downloading — this happens once and is cached in your browser. Voice assignments are ready now, playback will begin once the model finishes." })
        ]
      }
    ),
    kokoroError && (kokoroProvider == null ? void 0 : kokoroProvider.status) === "error" && /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: "rounded-lg border border-destructive/30 bg-destructive/5 px-4 py-3 space-y-2",
        "data-ocid": "kokoro-error-banner",
        children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-start gap-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "w-4 h-4 text-destructive flex-shrink-0 mt-0.5" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 min-w-0", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-medium text-destructive", children: "Kokoro TTS failed to load" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground mt-0.5 break-words", children: kokoroError }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground mt-1", children: kokoroIsActive ? "Falling back to browser voices for playback." : "Browser voices are active as fallback." })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            Button,
            {
              size: "sm",
              variant: "outline",
              onClick: retryKokoro,
              className: "flex-shrink-0 gap-1.5 border-destructive/40 text-destructive hover:bg-destructive/10",
              "data-ocid": "kokoro-retry",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(RefreshCw, { className: "w-3.5 h-3.5" }),
                "Retry"
              ]
            }
          )
        ] })
      }
    ),
    (kokoroProvider == null ? void 0 : kokoroProvider.status) === "ready" && kokoroIsActive && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: "flex items-center gap-2 text-xs text-success px-1",
        "data-ocid": "kokoro-ready-indicator",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheck, { className: "w-3.5 h-3.5 flex-shrink-0" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Kokoro neural voices active — 29 high-quality voices available" })
        ]
      }
    )
  ] });
}
function useStateMachine$1(initialState, machine) {
  return reactExports.useReducer((state, event) => {
    const nextState = machine[state][event];
    return nextState ?? state;
  }, initialState);
}
var Presence = (props) => {
  const { present, children } = props;
  const presence = usePresence(present);
  const child = typeof children === "function" ? children({ present: presence.isPresent }) : reactExports.Children.only(children);
  const ref = useComposedRefs(presence.ref, getElementRef(child));
  const forceMount = typeof children === "function";
  return forceMount || presence.isPresent ? reactExports.cloneElement(child, { ref }) : null;
};
Presence.displayName = "Presence";
function usePresence(present) {
  const [node, setNode] = reactExports.useState();
  const stylesRef = reactExports.useRef(null);
  const prevPresentRef = reactExports.useRef(present);
  const prevAnimationNameRef = reactExports.useRef("none");
  const initialState = present ? "mounted" : "unmounted";
  const [state, send] = useStateMachine$1(initialState, {
    mounted: {
      UNMOUNT: "unmounted",
      ANIMATION_OUT: "unmountSuspended"
    },
    unmountSuspended: {
      MOUNT: "mounted",
      ANIMATION_END: "unmounted"
    },
    unmounted: {
      MOUNT: "mounted"
    }
  });
  reactExports.useEffect(() => {
    const currentAnimationName = getAnimationName(stylesRef.current);
    prevAnimationNameRef.current = state === "mounted" ? currentAnimationName : "none";
  }, [state]);
  useLayoutEffect2(() => {
    const styles = stylesRef.current;
    const wasPresent = prevPresentRef.current;
    const hasPresentChanged = wasPresent !== present;
    if (hasPresentChanged) {
      const prevAnimationName = prevAnimationNameRef.current;
      const currentAnimationName = getAnimationName(styles);
      if (present) {
        send("MOUNT");
      } else if (currentAnimationName === "none" || (styles == null ? void 0 : styles.display) === "none") {
        send("UNMOUNT");
      } else {
        const isAnimating = prevAnimationName !== currentAnimationName;
        if (wasPresent && isAnimating) {
          send("ANIMATION_OUT");
        } else {
          send("UNMOUNT");
        }
      }
      prevPresentRef.current = present;
    }
  }, [present, send]);
  useLayoutEffect2(() => {
    if (node) {
      let timeoutId;
      const ownerWindow = node.ownerDocument.defaultView ?? window;
      const handleAnimationEnd = (event) => {
        const currentAnimationName = getAnimationName(stylesRef.current);
        const isCurrentAnimation = currentAnimationName.includes(CSS.escape(event.animationName));
        if (event.target === node && isCurrentAnimation) {
          send("ANIMATION_END");
          if (!prevPresentRef.current) {
            const currentFillMode = node.style.animationFillMode;
            node.style.animationFillMode = "forwards";
            timeoutId = ownerWindow.setTimeout(() => {
              if (node.style.animationFillMode === "forwards") {
                node.style.animationFillMode = currentFillMode;
              }
            });
          }
        }
      };
      const handleAnimationStart = (event) => {
        if (event.target === node) {
          prevAnimationNameRef.current = getAnimationName(stylesRef.current);
        }
      };
      node.addEventListener("animationstart", handleAnimationStart);
      node.addEventListener("animationcancel", handleAnimationEnd);
      node.addEventListener("animationend", handleAnimationEnd);
      return () => {
        ownerWindow.clearTimeout(timeoutId);
        node.removeEventListener("animationstart", handleAnimationStart);
        node.removeEventListener("animationcancel", handleAnimationEnd);
        node.removeEventListener("animationend", handleAnimationEnd);
      };
    } else {
      send("ANIMATION_END");
    }
  }, [node, send]);
  return {
    isPresent: ["mounted", "unmountSuspended"].includes(state),
    ref: reactExports.useCallback((node2) => {
      stylesRef.current = node2 ? getComputedStyle(node2) : null;
      setNode(node2);
    }, [])
  };
}
function getAnimationName(styles) {
  return (styles == null ? void 0 : styles.animationName) || "none";
}
function getElementRef(element) {
  var _a, _b;
  let getter = (_a = Object.getOwnPropertyDescriptor(element.props, "ref")) == null ? void 0 : _a.get;
  let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
  if (mayWarn) {
    return element.ref;
  }
  getter = (_b = Object.getOwnPropertyDescriptor(element, "ref")) == null ? void 0 : _b.get;
  mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
  if (mayWarn) {
    return element.props.ref;
  }
  return element.props.ref || element.ref;
}
function useStateMachine(initialState, machine) {
  return reactExports.useReducer((state, event) => {
    const nextState = machine[state][event];
    return nextState ?? state;
  }, initialState);
}
var SCROLL_AREA_NAME = "ScrollArea";
var [createScrollAreaContext] = createContextScope$1(SCROLL_AREA_NAME);
var [ScrollAreaProvider, useScrollAreaContext] = createScrollAreaContext(SCROLL_AREA_NAME);
var ScrollArea$1 = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeScrollArea,
      type = "hover",
      dir,
      scrollHideDelay = 600,
      ...scrollAreaProps
    } = props;
    const [scrollArea, setScrollArea] = reactExports.useState(null);
    const [viewport, setViewport] = reactExports.useState(null);
    const [content, setContent] = reactExports.useState(null);
    const [scrollbarX, setScrollbarX] = reactExports.useState(null);
    const [scrollbarY, setScrollbarY] = reactExports.useState(null);
    const [cornerWidth, setCornerWidth] = reactExports.useState(0);
    const [cornerHeight, setCornerHeight] = reactExports.useState(0);
    const [scrollbarXEnabled, setScrollbarXEnabled] = reactExports.useState(false);
    const [scrollbarYEnabled, setScrollbarYEnabled] = reactExports.useState(false);
    const composedRefs = useComposedRefs(forwardedRef, (node) => setScrollArea(node));
    const direction = useDirection(dir);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ScrollAreaProvider,
      {
        scope: __scopeScrollArea,
        type,
        dir: direction,
        scrollHideDelay,
        scrollArea,
        viewport,
        onViewportChange: setViewport,
        content,
        onContentChange: setContent,
        scrollbarX,
        onScrollbarXChange: setScrollbarX,
        scrollbarXEnabled,
        onScrollbarXEnabledChange: setScrollbarXEnabled,
        scrollbarY,
        onScrollbarYChange: setScrollbarY,
        scrollbarYEnabled,
        onScrollbarYEnabledChange: setScrollbarYEnabled,
        onCornerWidthChange: setCornerWidth,
        onCornerHeightChange: setCornerHeight,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          Primitive$1.div,
          {
            dir: direction,
            ...scrollAreaProps,
            ref: composedRefs,
            style: {
              position: "relative",
              // Pass corner sizes as CSS vars to reduce re-renders of context consumers
              ["--radix-scroll-area-corner-width"]: cornerWidth + "px",
              ["--radix-scroll-area-corner-height"]: cornerHeight + "px",
              ...props.style
            }
          }
        )
      }
    );
  }
);
ScrollArea$1.displayName = SCROLL_AREA_NAME;
var VIEWPORT_NAME = "ScrollAreaViewport";
var ScrollAreaViewport = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeScrollArea, children, nonce, ...viewportProps } = props;
    const context = useScrollAreaContext(VIEWPORT_NAME, __scopeScrollArea);
    const ref = reactExports.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, ref, context.onViewportChange);
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "style",
        {
          dangerouslySetInnerHTML: {
            __html: `[data-radix-scroll-area-viewport]{scrollbar-width:none;-ms-overflow-style:none;-webkit-overflow-scrolling:touch;}[data-radix-scroll-area-viewport]::-webkit-scrollbar{display:none}`
          },
          nonce
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Primitive$1.div,
        {
          "data-radix-scroll-area-viewport": "",
          ...viewportProps,
          ref: composedRefs,
          style: {
            /**
             * We don't support `visible` because the intention is to have at least one scrollbar
             * if this component is used and `visible` will behave like `auto` in that case
             * https://developer.mozilla.org/en-US/docs/Web/CSS/overflow#description
             *
             * We don't handle `auto` because the intention is for the native implementation
             * to be hidden if using this component. We just want to ensure the node is scrollable
             * so could have used either `scroll` or `auto` here. We picked `scroll` to prevent
             * the browser from having to work out whether to render native scrollbars or not,
             * we tell it to with the intention of hiding them in CSS.
             */
            overflowX: context.scrollbarXEnabled ? "scroll" : "hidden",
            overflowY: context.scrollbarYEnabled ? "scroll" : "hidden",
            ...props.style
          },
          children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { ref: context.onContentChange, style: { minWidth: "100%", display: "table" }, children })
        }
      )
    ] });
  }
);
ScrollAreaViewport.displayName = VIEWPORT_NAME;
var SCROLLBAR_NAME = "ScrollAreaScrollbar";
var ScrollAreaScrollbar = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { forceMount, ...scrollbarProps } = props;
    const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
    const { onScrollbarXEnabledChange, onScrollbarYEnabledChange } = context;
    const isHorizontal = props.orientation === "horizontal";
    reactExports.useEffect(() => {
      isHorizontal ? onScrollbarXEnabledChange(true) : onScrollbarYEnabledChange(true);
      return () => {
        isHorizontal ? onScrollbarXEnabledChange(false) : onScrollbarYEnabledChange(false);
      };
    }, [isHorizontal, onScrollbarXEnabledChange, onScrollbarYEnabledChange]);
    return context.type === "hover" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarHover, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context.type === "scroll" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarScroll, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context.type === "auto" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarAuto, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context.type === "always" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarVisible, { ...scrollbarProps, ref: forwardedRef }) : null;
  }
);
ScrollAreaScrollbar.displayName = SCROLLBAR_NAME;
var ScrollAreaScrollbarHover = reactExports.forwardRef((props, forwardedRef) => {
  const { forceMount, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const [visible, setVisible] = reactExports.useState(false);
  reactExports.useEffect(() => {
    const scrollArea = context.scrollArea;
    let hideTimer = 0;
    if (scrollArea) {
      const handlePointerEnter = () => {
        window.clearTimeout(hideTimer);
        setVisible(true);
      };
      const handlePointerLeave = () => {
        hideTimer = window.setTimeout(() => setVisible(false), context.scrollHideDelay);
      };
      scrollArea.addEventListener("pointerenter", handlePointerEnter);
      scrollArea.addEventListener("pointerleave", handlePointerLeave);
      return () => {
        window.clearTimeout(hideTimer);
        scrollArea.removeEventListener("pointerenter", handlePointerEnter);
        scrollArea.removeEventListener("pointerleave", handlePointerLeave);
      };
    }
  }, [context.scrollArea, context.scrollHideDelay]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || visible, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarAuto,
    {
      "data-state": visible ? "visible" : "hidden",
      ...scrollbarProps,
      ref: forwardedRef
    }
  ) });
});
var ScrollAreaScrollbarScroll = reactExports.forwardRef((props, forwardedRef) => {
  const { forceMount, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const isHorizontal = props.orientation === "horizontal";
  const debounceScrollEnd = useDebounceCallback(() => send("SCROLL_END"), 100);
  const [state, send] = useStateMachine("hidden", {
    hidden: {
      SCROLL: "scrolling"
    },
    scrolling: {
      SCROLL_END: "idle",
      POINTER_ENTER: "interacting"
    },
    interacting: {
      SCROLL: "interacting",
      POINTER_LEAVE: "idle"
    },
    idle: {
      HIDE: "hidden",
      SCROLL: "scrolling",
      POINTER_ENTER: "interacting"
    }
  });
  reactExports.useEffect(() => {
    if (state === "idle") {
      const hideTimer = window.setTimeout(() => send("HIDE"), context.scrollHideDelay);
      return () => window.clearTimeout(hideTimer);
    }
  }, [state, context.scrollHideDelay, send]);
  reactExports.useEffect(() => {
    const viewport = context.viewport;
    const scrollDirection = isHorizontal ? "scrollLeft" : "scrollTop";
    if (viewport) {
      let prevScrollPos = viewport[scrollDirection];
      const handleScroll = () => {
        const scrollPos = viewport[scrollDirection];
        const hasScrollInDirectionChanged = prevScrollPos !== scrollPos;
        if (hasScrollInDirectionChanged) {
          send("SCROLL");
          debounceScrollEnd();
        }
        prevScrollPos = scrollPos;
      };
      viewport.addEventListener("scroll", handleScroll);
      return () => viewport.removeEventListener("scroll", handleScroll);
    }
  }, [context.viewport, isHorizontal, send, debounceScrollEnd]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || state !== "hidden", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarVisible,
    {
      "data-state": state === "hidden" ? "hidden" : "visible",
      ...scrollbarProps,
      ref: forwardedRef,
      onPointerEnter: composeEventHandlers(props.onPointerEnter, () => send("POINTER_ENTER")),
      onPointerLeave: composeEventHandlers(props.onPointerLeave, () => send("POINTER_LEAVE"))
    }
  ) });
});
var ScrollAreaScrollbarAuto = reactExports.forwardRef((props, forwardedRef) => {
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const { forceMount, ...scrollbarProps } = props;
  const [visible, setVisible] = reactExports.useState(false);
  const isHorizontal = props.orientation === "horizontal";
  const handleResize = useDebounceCallback(() => {
    if (context.viewport) {
      const isOverflowX = context.viewport.offsetWidth < context.viewport.scrollWidth;
      const isOverflowY = context.viewport.offsetHeight < context.viewport.scrollHeight;
      setVisible(isHorizontal ? isOverflowX : isOverflowY);
    }
  }, 10);
  useResizeObserver(context.viewport, handleResize);
  useResizeObserver(context.content, handleResize);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || visible, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarVisible,
    {
      "data-state": visible ? "visible" : "hidden",
      ...scrollbarProps,
      ref: forwardedRef
    }
  ) });
});
var ScrollAreaScrollbarVisible = reactExports.forwardRef((props, forwardedRef) => {
  const { orientation = "vertical", ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const thumbRef = reactExports.useRef(null);
  const pointerOffsetRef = reactExports.useRef(0);
  const [sizes, setSizes] = reactExports.useState({
    content: 0,
    viewport: 0,
    scrollbar: { size: 0, paddingStart: 0, paddingEnd: 0 }
  });
  const thumbRatio = getThumbRatio(sizes.viewport, sizes.content);
  const commonProps = {
    ...scrollbarProps,
    sizes,
    onSizesChange: setSizes,
    hasThumb: Boolean(thumbRatio > 0 && thumbRatio < 1),
    onThumbChange: (thumb) => thumbRef.current = thumb,
    onThumbPointerUp: () => pointerOffsetRef.current = 0,
    onThumbPointerDown: (pointerPos) => pointerOffsetRef.current = pointerPos
  };
  function getScrollPosition(pointerPos, dir) {
    return getScrollPositionFromPointer(pointerPos, pointerOffsetRef.current, sizes, dir);
  }
  if (orientation === "horizontal") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ScrollAreaScrollbarX,
      {
        ...commonProps,
        ref: forwardedRef,
        onThumbPositionChange: () => {
          if (context.viewport && thumbRef.current) {
            const scrollPos = context.viewport.scrollLeft;
            const offset = getThumbOffsetFromScroll(scrollPos, sizes, context.dir);
            thumbRef.current.style.transform = `translate3d(${offset}px, 0, 0)`;
          }
        },
        onWheelScroll: (scrollPos) => {
          if (context.viewport) context.viewport.scrollLeft = scrollPos;
        },
        onDragScroll: (pointerPos) => {
          if (context.viewport) {
            context.viewport.scrollLeft = getScrollPosition(pointerPos, context.dir);
          }
        }
      }
    );
  }
  if (orientation === "vertical") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ScrollAreaScrollbarY,
      {
        ...commonProps,
        ref: forwardedRef,
        onThumbPositionChange: () => {
          if (context.viewport && thumbRef.current) {
            const scrollPos = context.viewport.scrollTop;
            const offset = getThumbOffsetFromScroll(scrollPos, sizes);
            thumbRef.current.style.transform = `translate3d(0, ${offset}px, 0)`;
          }
        },
        onWheelScroll: (scrollPos) => {
          if (context.viewport) context.viewport.scrollTop = scrollPos;
        },
        onDragScroll: (pointerPos) => {
          if (context.viewport) context.viewport.scrollTop = getScrollPosition(pointerPos);
        }
      }
    );
  }
  return null;
});
var ScrollAreaScrollbarX = reactExports.forwardRef((props, forwardedRef) => {
  const { sizes, onSizesChange, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const [computedStyle, setComputedStyle] = reactExports.useState();
  const ref = reactExports.useRef(null);
  const composeRefs = useComposedRefs(forwardedRef, ref, context.onScrollbarXChange);
  reactExports.useEffect(() => {
    if (ref.current) setComputedStyle(getComputedStyle(ref.current));
  }, [ref]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarImpl,
    {
      "data-orientation": "horizontal",
      ...scrollbarProps,
      ref: composeRefs,
      sizes,
      style: {
        bottom: 0,
        left: context.dir === "rtl" ? "var(--radix-scroll-area-corner-width)" : 0,
        right: context.dir === "ltr" ? "var(--radix-scroll-area-corner-width)" : 0,
        ["--radix-scroll-area-thumb-width"]: getThumbSize(sizes) + "px",
        ...props.style
      },
      onThumbPointerDown: (pointerPos) => props.onThumbPointerDown(pointerPos.x),
      onDragScroll: (pointerPos) => props.onDragScroll(pointerPos.x),
      onWheelScroll: (event, maxScrollPos) => {
        if (context.viewport) {
          const scrollPos = context.viewport.scrollLeft + event.deltaX;
          props.onWheelScroll(scrollPos);
          if (isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos)) {
            event.preventDefault();
          }
        }
      },
      onResize: () => {
        if (ref.current && context.viewport && computedStyle) {
          onSizesChange({
            content: context.viewport.scrollWidth,
            viewport: context.viewport.offsetWidth,
            scrollbar: {
              size: ref.current.clientWidth,
              paddingStart: toInt(computedStyle.paddingLeft),
              paddingEnd: toInt(computedStyle.paddingRight)
            }
          });
        }
      }
    }
  );
});
var ScrollAreaScrollbarY = reactExports.forwardRef((props, forwardedRef) => {
  const { sizes, onSizesChange, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const [computedStyle, setComputedStyle] = reactExports.useState();
  const ref = reactExports.useRef(null);
  const composeRefs = useComposedRefs(forwardedRef, ref, context.onScrollbarYChange);
  reactExports.useEffect(() => {
    if (ref.current) setComputedStyle(getComputedStyle(ref.current));
  }, [ref]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarImpl,
    {
      "data-orientation": "vertical",
      ...scrollbarProps,
      ref: composeRefs,
      sizes,
      style: {
        top: 0,
        right: context.dir === "ltr" ? 0 : void 0,
        left: context.dir === "rtl" ? 0 : void 0,
        bottom: "var(--radix-scroll-area-corner-height)",
        ["--radix-scroll-area-thumb-height"]: getThumbSize(sizes) + "px",
        ...props.style
      },
      onThumbPointerDown: (pointerPos) => props.onThumbPointerDown(pointerPos.y),
      onDragScroll: (pointerPos) => props.onDragScroll(pointerPos.y),
      onWheelScroll: (event, maxScrollPos) => {
        if (context.viewport) {
          const scrollPos = context.viewport.scrollTop + event.deltaY;
          props.onWheelScroll(scrollPos);
          if (isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos)) {
            event.preventDefault();
          }
        }
      },
      onResize: () => {
        if (ref.current && context.viewport && computedStyle) {
          onSizesChange({
            content: context.viewport.scrollHeight,
            viewport: context.viewport.offsetHeight,
            scrollbar: {
              size: ref.current.clientHeight,
              paddingStart: toInt(computedStyle.paddingTop),
              paddingEnd: toInt(computedStyle.paddingBottom)
            }
          });
        }
      }
    }
  );
});
var [ScrollbarProvider, useScrollbarContext] = createScrollAreaContext(SCROLLBAR_NAME);
var ScrollAreaScrollbarImpl = reactExports.forwardRef((props, forwardedRef) => {
  const {
    __scopeScrollArea,
    sizes,
    hasThumb,
    onThumbChange,
    onThumbPointerUp,
    onThumbPointerDown,
    onThumbPositionChange,
    onDragScroll,
    onWheelScroll,
    onResize,
    ...scrollbarProps
  } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, __scopeScrollArea);
  const [scrollbar, setScrollbar] = reactExports.useState(null);
  const composeRefs = useComposedRefs(forwardedRef, (node) => setScrollbar(node));
  const rectRef = reactExports.useRef(null);
  const prevWebkitUserSelectRef = reactExports.useRef("");
  const viewport = context.viewport;
  const maxScrollPos = sizes.content - sizes.viewport;
  const handleWheelScroll = useCallbackRef(onWheelScroll);
  const handleThumbPositionChange = useCallbackRef(onThumbPositionChange);
  const handleResize = useDebounceCallback(onResize, 10);
  function handleDragScroll(event) {
    if (rectRef.current) {
      const x = event.clientX - rectRef.current.left;
      const y = event.clientY - rectRef.current.top;
      onDragScroll({ x, y });
    }
  }
  reactExports.useEffect(() => {
    const handleWheel = (event) => {
      const element = event.target;
      const isScrollbarWheel = scrollbar == null ? void 0 : scrollbar.contains(element);
      if (isScrollbarWheel) handleWheelScroll(event, maxScrollPos);
    };
    document.addEventListener("wheel", handleWheel, { passive: false });
    return () => document.removeEventListener("wheel", handleWheel, { passive: false });
  }, [viewport, scrollbar, maxScrollPos, handleWheelScroll]);
  reactExports.useEffect(handleThumbPositionChange, [sizes, handleThumbPositionChange]);
  useResizeObserver(scrollbar, handleResize);
  useResizeObserver(context.content, handleResize);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollbarProvider,
    {
      scope: __scopeScrollArea,
      scrollbar,
      hasThumb,
      onThumbChange: useCallbackRef(onThumbChange),
      onThumbPointerUp: useCallbackRef(onThumbPointerUp),
      onThumbPositionChange: handleThumbPositionChange,
      onThumbPointerDown: useCallbackRef(onThumbPointerDown),
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        Primitive$1.div,
        {
          ...scrollbarProps,
          ref: composeRefs,
          style: { position: "absolute", ...scrollbarProps.style },
          onPointerDown: composeEventHandlers(props.onPointerDown, (event) => {
            const mainPointer = 0;
            if (event.button === mainPointer) {
              const element = event.target;
              element.setPointerCapture(event.pointerId);
              rectRef.current = scrollbar.getBoundingClientRect();
              prevWebkitUserSelectRef.current = document.body.style.webkitUserSelect;
              document.body.style.webkitUserSelect = "none";
              if (context.viewport) context.viewport.style.scrollBehavior = "auto";
              handleDragScroll(event);
            }
          }),
          onPointerMove: composeEventHandlers(props.onPointerMove, handleDragScroll),
          onPointerUp: composeEventHandlers(props.onPointerUp, (event) => {
            const element = event.target;
            if (element.hasPointerCapture(event.pointerId)) {
              element.releasePointerCapture(event.pointerId);
            }
            document.body.style.webkitUserSelect = prevWebkitUserSelectRef.current;
            if (context.viewport) context.viewport.style.scrollBehavior = "";
            rectRef.current = null;
          })
        }
      )
    }
  );
});
var THUMB_NAME = "ScrollAreaThumb";
var ScrollAreaThumb = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { forceMount, ...thumbProps } = props;
    const scrollbarContext = useScrollbarContext(THUMB_NAME, props.__scopeScrollArea);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || scrollbarContext.hasThumb, children: /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaThumbImpl, { ref: forwardedRef, ...thumbProps }) });
  }
);
var ScrollAreaThumbImpl = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeScrollArea, style, ...thumbProps } = props;
    const scrollAreaContext = useScrollAreaContext(THUMB_NAME, __scopeScrollArea);
    const scrollbarContext = useScrollbarContext(THUMB_NAME, __scopeScrollArea);
    const { onThumbPositionChange } = scrollbarContext;
    const composedRef = useComposedRefs(
      forwardedRef,
      (node) => scrollbarContext.onThumbChange(node)
    );
    const removeUnlinkedScrollListenerRef = reactExports.useRef(void 0);
    const debounceScrollEnd = useDebounceCallback(() => {
      if (removeUnlinkedScrollListenerRef.current) {
        removeUnlinkedScrollListenerRef.current();
        removeUnlinkedScrollListenerRef.current = void 0;
      }
    }, 100);
    reactExports.useEffect(() => {
      const viewport = scrollAreaContext.viewport;
      if (viewport) {
        const handleScroll = () => {
          debounceScrollEnd();
          if (!removeUnlinkedScrollListenerRef.current) {
            const listener = addUnlinkedScrollListener(viewport, onThumbPositionChange);
            removeUnlinkedScrollListenerRef.current = listener;
            onThumbPositionChange();
          }
        };
        onThumbPositionChange();
        viewport.addEventListener("scroll", handleScroll);
        return () => viewport.removeEventListener("scroll", handleScroll);
      }
    }, [scrollAreaContext.viewport, debounceScrollEnd, onThumbPositionChange]);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive$1.div,
      {
        "data-state": scrollbarContext.hasThumb ? "visible" : "hidden",
        ...thumbProps,
        ref: composedRef,
        style: {
          width: "var(--radix-scroll-area-thumb-width)",
          height: "var(--radix-scroll-area-thumb-height)",
          ...style
        },
        onPointerDownCapture: composeEventHandlers(props.onPointerDownCapture, (event) => {
          const thumb = event.target;
          const thumbRect = thumb.getBoundingClientRect();
          const x = event.clientX - thumbRect.left;
          const y = event.clientY - thumbRect.top;
          scrollbarContext.onThumbPointerDown({ x, y });
        }),
        onPointerUp: composeEventHandlers(props.onPointerUp, scrollbarContext.onThumbPointerUp)
      }
    );
  }
);
ScrollAreaThumb.displayName = THUMB_NAME;
var CORNER_NAME = "ScrollAreaCorner";
var ScrollAreaCorner = reactExports.forwardRef(
  (props, forwardedRef) => {
    const context = useScrollAreaContext(CORNER_NAME, props.__scopeScrollArea);
    const hasBothScrollbarsVisible = Boolean(context.scrollbarX && context.scrollbarY);
    const hasCorner = context.type !== "scroll" && hasBothScrollbarsVisible;
    return hasCorner ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaCornerImpl, { ...props, ref: forwardedRef }) : null;
  }
);
ScrollAreaCorner.displayName = CORNER_NAME;
var ScrollAreaCornerImpl = reactExports.forwardRef((props, forwardedRef) => {
  const { __scopeScrollArea, ...cornerProps } = props;
  const context = useScrollAreaContext(CORNER_NAME, __scopeScrollArea);
  const [width, setWidth] = reactExports.useState(0);
  const [height, setHeight] = reactExports.useState(0);
  const hasSize = Boolean(width && height);
  useResizeObserver(context.scrollbarX, () => {
    var _a;
    const height2 = ((_a = context.scrollbarX) == null ? void 0 : _a.offsetHeight) || 0;
    context.onCornerHeightChange(height2);
    setHeight(height2);
  });
  useResizeObserver(context.scrollbarY, () => {
    var _a;
    const width2 = ((_a = context.scrollbarY) == null ? void 0 : _a.offsetWidth) || 0;
    context.onCornerWidthChange(width2);
    setWidth(width2);
  });
  return hasSize ? /* @__PURE__ */ jsxRuntimeExports.jsx(
    Primitive$1.div,
    {
      ...cornerProps,
      ref: forwardedRef,
      style: {
        width,
        height,
        position: "absolute",
        right: context.dir === "ltr" ? 0 : void 0,
        left: context.dir === "rtl" ? 0 : void 0,
        bottom: 0,
        ...props.style
      }
    }
  ) : null;
});
function toInt(value) {
  return value ? parseInt(value, 10) : 0;
}
function getThumbRatio(viewportSize, contentSize) {
  const ratio = viewportSize / contentSize;
  return isNaN(ratio) ? 0 : ratio;
}
function getThumbSize(sizes) {
  const ratio = getThumbRatio(sizes.viewport, sizes.content);
  const scrollbarPadding = sizes.scrollbar.paddingStart + sizes.scrollbar.paddingEnd;
  const thumbSize = (sizes.scrollbar.size - scrollbarPadding) * ratio;
  return Math.max(thumbSize, 18);
}
function getScrollPositionFromPointer(pointerPos, pointerOffset, sizes, dir = "ltr") {
  const thumbSizePx = getThumbSize(sizes);
  const thumbCenter = thumbSizePx / 2;
  const offset = pointerOffset || thumbCenter;
  const thumbOffsetFromEnd = thumbSizePx - offset;
  const minPointerPos = sizes.scrollbar.paddingStart + offset;
  const maxPointerPos = sizes.scrollbar.size - sizes.scrollbar.paddingEnd - thumbOffsetFromEnd;
  const maxScrollPos = sizes.content - sizes.viewport;
  const scrollRange = dir === "ltr" ? [0, maxScrollPos] : [maxScrollPos * -1, 0];
  const interpolate = linearScale([minPointerPos, maxPointerPos], scrollRange);
  return interpolate(pointerPos);
}
function getThumbOffsetFromScroll(scrollPos, sizes, dir = "ltr") {
  const thumbSizePx = getThumbSize(sizes);
  const scrollbarPadding = sizes.scrollbar.paddingStart + sizes.scrollbar.paddingEnd;
  const scrollbar = sizes.scrollbar.size - scrollbarPadding;
  const maxScrollPos = sizes.content - sizes.viewport;
  const maxThumbPos = scrollbar - thumbSizePx;
  const scrollClampRange = dir === "ltr" ? [0, maxScrollPos] : [maxScrollPos * -1, 0];
  const scrollWithoutMomentum = clamp(scrollPos, scrollClampRange);
  const interpolate = linearScale([0, maxScrollPos], [0, maxThumbPos]);
  return interpolate(scrollWithoutMomentum);
}
function linearScale(input, output) {
  return (value) => {
    if (input[0] === input[1] || output[0] === output[1]) return output[0];
    const ratio = (output[1] - output[0]) / (input[1] - input[0]);
    return output[0] + ratio * (value - input[0]);
  };
}
function isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos) {
  return scrollPos > 0 && scrollPos < maxScrollPos;
}
var addUnlinkedScrollListener = (node, handler = () => {
}) => {
  let prevPosition = { left: node.scrollLeft, top: node.scrollTop };
  let rAF = 0;
  (function loop() {
    const position = { left: node.scrollLeft, top: node.scrollTop };
    const isHorizontalScroll = prevPosition.left !== position.left;
    const isVerticalScroll = prevPosition.top !== position.top;
    if (isHorizontalScroll || isVerticalScroll) handler();
    prevPosition = position;
    rAF = window.requestAnimationFrame(loop);
  })();
  return () => window.cancelAnimationFrame(rAF);
};
function useDebounceCallback(callback, delay) {
  const handleCallback = useCallbackRef(callback);
  const debounceTimerRef = reactExports.useRef(0);
  reactExports.useEffect(() => () => window.clearTimeout(debounceTimerRef.current), []);
  return reactExports.useCallback(() => {
    window.clearTimeout(debounceTimerRef.current);
    debounceTimerRef.current = window.setTimeout(handleCallback, delay);
  }, [handleCallback, delay]);
}
function useResizeObserver(element, onResize) {
  const handleResize = useCallbackRef(onResize);
  useLayoutEffect2(() => {
    let rAF = 0;
    if (element) {
      const resizeObserver = new ResizeObserver(() => {
        cancelAnimationFrame(rAF);
        rAF = window.requestAnimationFrame(handleResize);
      });
      resizeObserver.observe(element);
      return () => {
        window.cancelAnimationFrame(rAF);
        resizeObserver.unobserve(element);
      };
    }
  }, [element, handleResize]);
}
var Root$1 = ScrollArea$1;
var Viewport = ScrollAreaViewport;
var Corner = ScrollAreaCorner;
function ScrollArea({
  className,
  children,
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    Root$1,
    {
      "data-slot": "scroll-area",
      className: cn("relative", className),
      ...props,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Viewport,
          {
            "data-slot": "scroll-area-viewport",
            className: "focus-visible:ring-ring/50 size-full rounded-[inherit] transition-[color,box-shadow] outline-none focus-visible:ring-[3px] focus-visible:outline-1",
            children
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollBar, {}),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Corner, {})
      ]
    }
  );
}
function ScrollBar({
  className,
  orientation = "vertical",
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbar,
    {
      "data-slot": "scroll-area-scrollbar",
      orientation,
      className: cn(
        "flex touch-none p-px transition-colors select-none",
        orientation === "vertical" && "h-full w-2.5 border-l border-l-transparent",
        orientation === "horizontal" && "h-2.5 flex-col border-t border-t-transparent",
        className
      ),
      ...props,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        ScrollAreaThumb,
        {
          "data-slot": "scroll-area-thumb",
          className: "bg-border relative flex-1 rounded-full"
        }
      )
    }
  );
}
function shuffleArray(arr) {
  const a = [...arr];
  for (let i = a.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [a[i], a[j]] = [a[j], a[i]];
  }
  return a;
}
function distinctPitches(count) {
  return Array.from({ length: count }, (_, i) => {
    const raw = 0.6 + 0.9 / Math.max(count - 1, 1) * i;
    return Math.round(raw * 10) / 10;
  });
}
function distinctRates(count) {
  const slow = [0.75, 0.8, 0.85, 0.9, 0.95];
  const fast = [1.1, 1.15, 1.2, 1.25, 1.3, 1.4];
  return Array.from(
    { length: count },
    (_, i) => i % 2 === 0 ? slow[Math.floor(i / 2) % slow.length] : fast[Math.floor(i / 2) % fast.length]
  );
}
function VoiceStudio({
  screenplayId,
  characters,
  availableVoices,
  voiceAssignments,
  defaultVoiceAssignments,
  onVoiceAssignmentsChange,
  savedSettings,
  scriptLanguage
}) {
  var _a;
  const saveVoiceSettings = useSaveVoiceSettings();
  const hasLoadedSaved = reactExports.useRef(false);
  const [copiedSettings, setCopiedSettings] = reactExports.useState(
    null
  );
  const [globalLanguage, setGlobalLanguage] = reactExports.useState(scriptLanguage);
  const [selectedChar, setSelectedChar] = reactExports.useState(null);
  const { activeProvider } = useTTSProvider();
  reactExports.useEffect(() => {
    if (savedSettings.length > 0 && !hasLoadedSaved.current) {
      hasLoadedSaved.current = true;
      const loaded = {};
      for (const s of savedSettings) {
        loaded[s.character] = {
          voiceName: s.voiceUri || void 0,
          pitch: s.pitch,
          rate: s.rate,
          language: globalLanguage
        };
      }
      onVoiceAssignmentsChange(loaded);
    }
  }, [savedSettings, globalLanguage, onVoiceAssignmentsChange]);
  reactExports.useEffect(() => {
    if (characters.length > 0 && !selectedChar) {
      setSelectedChar(characters[0].name);
    }
  }, [characters, selectedChar]);
  const providerVoices = (activeProvider == null ? void 0 : activeProvider.voices) ?? [];
  const displayVoices = providerVoices.length > 0 ? providerVoices.map((v) => ({ name: v.name, id: v.id, lang: v.lang })) : availableVoices.map((v) => ({
    name: v.name,
    id: v.name,
    lang: v.lang
  }));
  const availableLanguages = Array.from(
    new Set(displayVoices.map((v) => v.lang))
  ).sort();
  const getAssignment = (name) => voiceAssignments[name] ?? defaultVoiceAssignments[name] ?? {
    pitch: 1,
    rate: 1,
    language: globalLanguage
  };
  const updateAssignment = (character, updates) => {
    const current = getAssignment(character);
    onVoiceAssignmentsChange({
      ...voiceAssignments,
      [character]: { ...current, ...updates }
    });
  };
  const persistSettings = async (assignments) => {
    const settings = Object.entries(assignments).map(
      ([char, a]) => ({
        character: char,
        voiceUri: a.voiceName ?? "",
        pitch: a.pitch,
        rate: a.rate,
        language: a.language ?? globalLanguage
      })
    );
    try {
      await saveVoiceSettings.mutateAsync({ screenplayId, settings });
      ue.success("Voice settings saved");
    } catch {
      ue.error("Failed to save voice settings");
    }
  };
  const randomizeAll = () => {
    const langVoices = displayVoices.filter((v) => v.lang === globalLanguage);
    const voicePool = langVoices.length > 0 ? langVoices : displayVoices;
    const shuffled = shuffleArray(voicePool);
    const pitches = distinctPitches(characters.length);
    const rates = distinctRates(characters.length);
    const assignments = {};
    characters.forEach((char, i) => {
      assignments[char.name] = {
        voiceName: shuffled[i % shuffled.length].id,
        pitch: pitches[i],
        rate: rates[i],
        language: globalLanguage
        // always use top-level language, never randomize
      };
    });
    onVoiceAssignmentsChange(assignments);
    persistSettings(assignments);
    ue.success("All voices randomized!");
  };
  const activeChar = selectedChar ?? ((_a = characters[0]) == null ? void 0 : _a.name) ?? null;
  const activeCharData = characters.find((c) => c.name === activeChar);
  const activeColorIdx = activeChar ? getCharacterColor(characters.findIndex((c) => c.name === activeChar)) : 0;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-4", "data-ocid": "voice-studio", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs(Card, { className: "border-primary/20 bg-gradient-to-r from-primary/5 to-accent/5", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(CardHeader, { className: "pb-3", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between gap-4 flex-wrap", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(CardTitle, { className: "font-display text-xl flex items-center gap-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Sparkles, { className: "w-5 h-5 text-primary" }),
            "Voice Studio"
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(CardDescription, { children: "Choose your voice engine and assign distinct voices to each character" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 flex-wrap", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Globe, { className: "w-4 h-4 text-muted-foreground" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { htmlFor: "global-lang", className: "sr-only", children: "Script Language" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              Select,
              {
                value: globalLanguage,
                onValueChange: (v) => setGlobalLanguage(v),
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    SelectTrigger,
                    {
                      id: "global-lang",
                      className: "w-36 h-8 text-xs",
                      "data-ocid": "language-select",
                      children: /* @__PURE__ */ jsxRuntimeExports.jsx(SelectValue, {})
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(SelectContent, { children: availableLanguages.map((lang) => /* @__PURE__ */ jsxRuntimeExports.jsx(SelectItem, { value: lang, className: "text-xs", children: lang }, lang)) })
                ]
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            Button,
            {
              size: "sm",
              variant: "outline",
              onClick: randomizeAll,
              className: "gap-1.5",
              "data-ocid": "randomize-all",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Shuffle, { className: "w-3.5 h-3.5" }),
                "Randomize"
              ]
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            Button,
            {
              size: "sm",
              onClick: () => persistSettings(voiceAssignments),
              disabled: saveVoiceSettings.isPending,
              className: "gap-1.5",
              "data-ocid": "save-voice-settings",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Save, { className: "w-3.5 h-3.5" }),
                "Save"
              ]
            }
          )
        ] })
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(CardContent, { className: "pt-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx(TTSProviderSelector, {}) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "grid grid-cols-[240px_1fr] gap-4", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Card, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(CardContent, { className: "p-2", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollArea, { className: "h-[520px] scrollbar-thin", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "space-y-1 p-1", children: characters.map((char, i) => {
        const colorIdx = getCharacterColor(i);
        const color = CHARACTER_COLORS[colorIdx];
        const a = getAssignment(char.name);
        const hasVoice = !!a.voiceName;
        const isSelected = char.name === activeChar;
        return /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "button",
          {
            type: "button",
            onClick: () => setSelectedChar(char.name),
            className: `w-full flex items-center gap-3 px-3 py-2.5 rounded-lg text-left transition-smooth ${isSelected ? `${color.bg} ${color.border} border` : "hover:bg-muted/50"}`,
            "data-ocid": `char-${char.name.toLowerCase().replace(/\s+/g, "-")}`,
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "span",
                {
                  className: `character-badge w-8 h-8 text-[11px] flex-shrink-0 ${color.bg} ${color.text} border ${color.border}`,
                  children: getInitials(char.name)
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-w-0 flex-1", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "p",
                  {
                    className: `text-sm font-medium truncate ${isSelected ? color.text : ""}`,
                    children: char.name
                  }
                ),
                /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: char.name === "Narrator" ? "narration" : `${char.count} lines` })
              ] }),
              hasVoice ? /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheck, { className: "w-3.5 h-3.5 text-success flex-shrink-0" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(CircleAlert, { className: "w-3.5 h-3.5 text-muted-foreground flex-shrink-0" })
            ]
          },
          char.name
        );
      }) }) }) }) }),
      activeChar && /* @__PURE__ */ jsxRuntimeExports.jsx(
        CharacterVoiceSettings,
        {
          characterName: activeChar,
          dialogueCount: (activeCharData == null ? void 0 : activeCharData.count) ?? 0,
          colorIndex: activeColorIdx,
          assignment: getAssignment(activeChar),
          voices: displayVoices,
          availableLanguages,
          globalLanguage,
          activeProvider: (activeProvider == null ? void 0 : activeProvider.id) ?? "web-speech",
          copiedSettings,
          onUpdate: (updates) => updateAssignment(activeChar, updates),
          onCopy: (settings) => setCopiedSettings(settings),
          onPaste: () => {
            if (copiedSettings) updateAssignment(activeChar, copiedSettings);
          },
          webSpeechVoices: availableVoices
        }
      )
    ] })
  ] });
}
var ENTRY_FOCUS = "rovingFocusGroup.onEntryFocus";
var EVENT_OPTIONS = { bubbles: false, cancelable: true };
var GROUP_NAME = "RovingFocusGroup";
var [Collection, useCollection, createCollectionScope] = createCollection(GROUP_NAME);
var [createRovingFocusGroupContext, createRovingFocusGroupScope] = createContextScope$1(
  GROUP_NAME,
  [createCollectionScope]
);
var [RovingFocusProvider, useRovingFocusContext] = createRovingFocusGroupContext(GROUP_NAME);
var RovingFocusGroup = reactExports.forwardRef(
  (props, forwardedRef) => {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(Collection.Provider, { scope: props.__scopeRovingFocusGroup, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Collection.Slot, { scope: props.__scopeRovingFocusGroup, children: /* @__PURE__ */ jsxRuntimeExports.jsx(RovingFocusGroupImpl, { ...props, ref: forwardedRef }) }) });
  }
);
RovingFocusGroup.displayName = GROUP_NAME;
var RovingFocusGroupImpl = reactExports.forwardRef((props, forwardedRef) => {
  const {
    __scopeRovingFocusGroup,
    orientation,
    loop = false,
    dir,
    currentTabStopId: currentTabStopIdProp,
    defaultCurrentTabStopId,
    onCurrentTabStopIdChange,
    onEntryFocus,
    preventScrollOnEntryFocus = false,
    ...groupProps
  } = props;
  const ref = reactExports.useRef(null);
  const composedRefs = useComposedRefs(forwardedRef, ref);
  const direction = useDirection(dir);
  const [currentTabStopId, setCurrentTabStopId] = useControllableState({
    prop: currentTabStopIdProp,
    defaultProp: defaultCurrentTabStopId ?? null,
    onChange: onCurrentTabStopIdChange,
    caller: GROUP_NAME
  });
  const [isTabbingBackOut, setIsTabbingBackOut] = reactExports.useState(false);
  const handleEntryFocus = useCallbackRef(onEntryFocus);
  const getItems = useCollection(__scopeRovingFocusGroup);
  const isClickFocusRef = reactExports.useRef(false);
  const [focusableItemsCount, setFocusableItemsCount] = reactExports.useState(0);
  reactExports.useEffect(() => {
    const node = ref.current;
    if (node) {
      node.addEventListener(ENTRY_FOCUS, handleEntryFocus);
      return () => node.removeEventListener(ENTRY_FOCUS, handleEntryFocus);
    }
  }, [handleEntryFocus]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    RovingFocusProvider,
    {
      scope: __scopeRovingFocusGroup,
      orientation,
      dir: direction,
      loop,
      currentTabStopId,
      onItemFocus: reactExports.useCallback(
        (tabStopId) => setCurrentTabStopId(tabStopId),
        [setCurrentTabStopId]
      ),
      onItemShiftTab: reactExports.useCallback(() => setIsTabbingBackOut(true), []),
      onFocusableItemAdd: reactExports.useCallback(
        () => setFocusableItemsCount((prevCount) => prevCount + 1),
        []
      ),
      onFocusableItemRemove: reactExports.useCallback(
        () => setFocusableItemsCount((prevCount) => prevCount - 1),
        []
      ),
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        Primitive$1.div,
        {
          tabIndex: isTabbingBackOut || focusableItemsCount === 0 ? -1 : 0,
          "data-orientation": orientation,
          ...groupProps,
          ref: composedRefs,
          style: { outline: "none", ...props.style },
          onMouseDown: composeEventHandlers(props.onMouseDown, () => {
            isClickFocusRef.current = true;
          }),
          onFocus: composeEventHandlers(props.onFocus, (event) => {
            const isKeyboardFocus = !isClickFocusRef.current;
            if (event.target === event.currentTarget && isKeyboardFocus && !isTabbingBackOut) {
              const entryFocusEvent = new CustomEvent(ENTRY_FOCUS, EVENT_OPTIONS);
              event.currentTarget.dispatchEvent(entryFocusEvent);
              if (!entryFocusEvent.defaultPrevented) {
                const items = getItems().filter((item) => item.focusable);
                const activeItem = items.find((item) => item.active);
                const currentItem = items.find((item) => item.id === currentTabStopId);
                const candidateItems = [activeItem, currentItem, ...items].filter(
                  Boolean
                );
                const candidateNodes = candidateItems.map((item) => item.ref.current);
                focusFirst(candidateNodes, preventScrollOnEntryFocus);
              }
            }
            isClickFocusRef.current = false;
          }),
          onBlur: composeEventHandlers(props.onBlur, () => setIsTabbingBackOut(false))
        }
      )
    }
  );
});
var ITEM_NAME = "RovingFocusGroupItem";
var RovingFocusGroupItem = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeRovingFocusGroup,
      focusable = true,
      active = false,
      tabStopId,
      children,
      ...itemProps
    } = props;
    const autoId = useId();
    const id = tabStopId || autoId;
    const context = useRovingFocusContext(ITEM_NAME, __scopeRovingFocusGroup);
    const isCurrentTabStop = context.currentTabStopId === id;
    const getItems = useCollection(__scopeRovingFocusGroup);
    const { onFocusableItemAdd, onFocusableItemRemove, currentTabStopId } = context;
    reactExports.useEffect(() => {
      if (focusable) {
        onFocusableItemAdd();
        return () => onFocusableItemRemove();
      }
    }, [focusable, onFocusableItemAdd, onFocusableItemRemove]);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Collection.ItemSlot,
      {
        scope: __scopeRovingFocusGroup,
        id,
        focusable,
        active,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          Primitive$1.span,
          {
            tabIndex: isCurrentTabStop ? 0 : -1,
            "data-orientation": context.orientation,
            ...itemProps,
            ref: forwardedRef,
            onMouseDown: composeEventHandlers(props.onMouseDown, (event) => {
              if (!focusable) event.preventDefault();
              else context.onItemFocus(id);
            }),
            onFocus: composeEventHandlers(props.onFocus, () => context.onItemFocus(id)),
            onKeyDown: composeEventHandlers(props.onKeyDown, (event) => {
              if (event.key === "Tab" && event.shiftKey) {
                context.onItemShiftTab();
                return;
              }
              if (event.target !== event.currentTarget) return;
              const focusIntent = getFocusIntent(event, context.orientation, context.dir);
              if (focusIntent !== void 0) {
                if (event.metaKey || event.ctrlKey || event.altKey || event.shiftKey) return;
                event.preventDefault();
                const items = getItems().filter((item) => item.focusable);
                let candidateNodes = items.map((item) => item.ref.current);
                if (focusIntent === "last") candidateNodes.reverse();
                else if (focusIntent === "prev" || focusIntent === "next") {
                  if (focusIntent === "prev") candidateNodes.reverse();
                  const currentIndex = candidateNodes.indexOf(event.currentTarget);
                  candidateNodes = context.loop ? wrapArray(candidateNodes, currentIndex + 1) : candidateNodes.slice(currentIndex + 1);
                }
                setTimeout(() => focusFirst(candidateNodes));
              }
            }),
            children: typeof children === "function" ? children({ isCurrentTabStop, hasTabStop: currentTabStopId != null }) : children
          }
        )
      }
    );
  }
);
RovingFocusGroupItem.displayName = ITEM_NAME;
var MAP_KEY_TO_FOCUS_INTENT = {
  ArrowLeft: "prev",
  ArrowUp: "prev",
  ArrowRight: "next",
  ArrowDown: "next",
  PageUp: "first",
  Home: "first",
  PageDown: "last",
  End: "last"
};
function getDirectionAwareKey(key, dir) {
  if (dir !== "rtl") return key;
  return key === "ArrowLeft" ? "ArrowRight" : key === "ArrowRight" ? "ArrowLeft" : key;
}
function getFocusIntent(event, orientation, dir) {
  const key = getDirectionAwareKey(event.key, dir);
  if (orientation === "vertical" && ["ArrowLeft", "ArrowRight"].includes(key)) return void 0;
  if (orientation === "horizontal" && ["ArrowUp", "ArrowDown"].includes(key)) return void 0;
  return MAP_KEY_TO_FOCUS_INTENT[key];
}
function focusFirst(candidates, preventScroll = false) {
  const PREVIOUSLY_FOCUSED_ELEMENT = document.activeElement;
  for (const candidate of candidates) {
    if (candidate === PREVIOUSLY_FOCUSED_ELEMENT) return;
    candidate.focus({ preventScroll });
    if (document.activeElement !== PREVIOUSLY_FOCUSED_ELEMENT) return;
  }
}
function wrapArray(array, startIndex) {
  return array.map((_, index) => array[(startIndex + index) % array.length]);
}
var Root = RovingFocusGroup;
var Item = RovingFocusGroupItem;
var TABS_NAME = "Tabs";
var [createTabsContext] = createContextScope$1(TABS_NAME, [
  createRovingFocusGroupScope
]);
var useRovingFocusGroupScope = createRovingFocusGroupScope();
var [TabsProvider, useTabsContext] = createTabsContext(TABS_NAME);
var Tabs$1 = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeTabs,
      value: valueProp,
      onValueChange,
      defaultValue,
      orientation = "horizontal",
      dir,
      activationMode = "automatic",
      ...tabsProps
    } = props;
    const direction = useDirection(dir);
    const [value, setValue] = useControllableState({
      prop: valueProp,
      onChange: onValueChange,
      defaultProp: defaultValue ?? "",
      caller: TABS_NAME
    });
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      TabsProvider,
      {
        scope: __scopeTabs,
        baseId: useId(),
        value,
        onValueChange: setValue,
        orientation,
        dir: direction,
        activationMode,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          Primitive$1.div,
          {
            dir: direction,
            "data-orientation": orientation,
            ...tabsProps,
            ref: forwardedRef
          }
        )
      }
    );
  }
);
Tabs$1.displayName = TABS_NAME;
var TAB_LIST_NAME = "TabsList";
var TabsList$1 = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeTabs, loop = true, ...listProps } = props;
    const context = useTabsContext(TAB_LIST_NAME, __scopeTabs);
    const rovingFocusGroupScope = useRovingFocusGroupScope(__scopeTabs);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Root,
      {
        asChild: true,
        ...rovingFocusGroupScope,
        orientation: context.orientation,
        dir: context.dir,
        loop,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          Primitive$1.div,
          {
            role: "tablist",
            "aria-orientation": context.orientation,
            ...listProps,
            ref: forwardedRef
          }
        )
      }
    );
  }
);
TabsList$1.displayName = TAB_LIST_NAME;
var TRIGGER_NAME = "TabsTrigger";
var TabsTrigger$1 = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeTabs, value, disabled = false, ...triggerProps } = props;
    const context = useTabsContext(TRIGGER_NAME, __scopeTabs);
    const rovingFocusGroupScope = useRovingFocusGroupScope(__scopeTabs);
    const triggerId = makeTriggerId(context.baseId, value);
    const contentId = makeContentId(context.baseId, value);
    const isSelected = value === context.value;
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Item,
      {
        asChild: true,
        ...rovingFocusGroupScope,
        focusable: !disabled,
        active: isSelected,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          Primitive$1.button,
          {
            type: "button",
            role: "tab",
            "aria-selected": isSelected,
            "aria-controls": contentId,
            "data-state": isSelected ? "active" : "inactive",
            "data-disabled": disabled ? "" : void 0,
            disabled,
            id: triggerId,
            ...triggerProps,
            ref: forwardedRef,
            onMouseDown: composeEventHandlers(props.onMouseDown, (event) => {
              if (!disabled && event.button === 0 && event.ctrlKey === false) {
                context.onValueChange(value);
              } else {
                event.preventDefault();
              }
            }),
            onKeyDown: composeEventHandlers(props.onKeyDown, (event) => {
              if ([" ", "Enter"].includes(event.key)) context.onValueChange(value);
            }),
            onFocus: composeEventHandlers(props.onFocus, () => {
              const isAutomaticActivation = context.activationMode !== "manual";
              if (!isSelected && !disabled && isAutomaticActivation) {
                context.onValueChange(value);
              }
            })
          }
        )
      }
    );
  }
);
TabsTrigger$1.displayName = TRIGGER_NAME;
var CONTENT_NAME = "TabsContent";
var TabsContent$1 = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeTabs, value, forceMount, children, ...contentProps } = props;
    const context = useTabsContext(CONTENT_NAME, __scopeTabs);
    const triggerId = makeTriggerId(context.baseId, value);
    const contentId = makeContentId(context.baseId, value);
    const isSelected = value === context.value;
    const isMountAnimationPreventedRef = reactExports.useRef(isSelected);
    reactExports.useEffect(() => {
      const rAF = requestAnimationFrame(() => isMountAnimationPreventedRef.current = false);
      return () => cancelAnimationFrame(rAF);
    }, []);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || isSelected, children: ({ present }) => /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive$1.div,
      {
        "data-state": isSelected ? "active" : "inactive",
        "data-orientation": context.orientation,
        role: "tabpanel",
        "aria-labelledby": triggerId,
        hidden: !present,
        id: contentId,
        tabIndex: 0,
        ...contentProps,
        ref: forwardedRef,
        style: {
          ...props.style,
          animationDuration: isMountAnimationPreventedRef.current ? "0s" : void 0
        },
        children: present && children
      }
    ) });
  }
);
TabsContent$1.displayName = CONTENT_NAME;
function makeTriggerId(baseId, value) {
  return `${baseId}-trigger-${value}`;
}
function makeContentId(baseId, value) {
  return `${baseId}-content-${value}`;
}
var Root2 = Tabs$1;
var List = TabsList$1;
var Trigger = TabsTrigger$1;
var Content = TabsContent$1;
function Tabs({
  className,
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Root2,
    {
      "data-slot": "tabs",
      className: cn("flex flex-col gap-2", className),
      ...props
    }
  );
}
function TabsList({
  className,
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    List,
    {
      "data-slot": "tabs-list",
      className: cn(
        "bg-muted text-muted-foreground inline-flex h-9 w-fit items-center justify-center rounded-lg p-[3px]",
        className
      ),
      ...props
    }
  );
}
function TabsTrigger({
  className,
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Trigger,
    {
      "data-slot": "tabs-trigger",
      className: cn(
        "data-[state=active]:bg-background dark:data-[state=active]:text-foreground focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:outline-ring dark:data-[state=active]:border-input dark:data-[state=active]:bg-input/30 text-foreground dark:text-muted-foreground inline-flex h-[calc(100%-1px)] flex-1 items-center justify-center gap-1.5 rounded-md border border-transparent px-2 py-1 text-sm font-medium whitespace-nowrap transition-[color,box-shadow] focus-visible:ring-[3px] focus-visible:outline-1 disabled:pointer-events-none disabled:opacity-50 data-[state=active]:shadow-sm [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4",
        className
      ),
      ...props
    }
  );
}
function TabsContent({
  className,
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Content,
    {
      "data-slot": "tabs-content",
      className: cn("flex-1 outline-none", className),
      ...props
    }
  );
}
function parseScreenplay(text) {
  const lines = text.split("\n");
  const elements = [];
  let currentCharacter;
  for (let i = 0; i < lines.length; i++) {
    const line = lines[i];
    const trimmed = line.trim();
    if (!trimmed) {
      currentCharacter = void 0;
      continue;
    }
    if (/^\.(?!\.)/.test(trimmed)) {
      elements.push({ type: "scene_heading", text: trimmed.slice(1) });
      currentCharacter = void 0;
      continue;
    }
    if (/^(INT|EXT|INT\.\/EXT|INT\/EXT|I\/E)[\.\s\-]/i.test(trimmed)) {
      elements.push({ type: "scene_heading", text: trimmed });
      currentCharacter = void 0;
      continue;
    }
    if (/^[A-Z\s]+TO:\s*$/.test(trimmed) || /^(FADE OUT|FADE IN|CUT TO|SMASH CUT|MATCH CUT):?\s*$/.test(trimmed)) {
      elements.push({ type: "transition", text: trimmed });
      currentCharacter = void 0;
      continue;
    }
    if (trimmed.startsWith("!")) {
      elements.push({ type: "action", text: trimmed.slice(1) });
      currentCharacter = void 0;
      continue;
    }
    const charMatch = /^([A-Z][A-Z0-9\s\-'\.]+?)(\s*\([^)]+\))?\s*$/.exec(
      trimmed
    );
    if (charMatch && trimmed.length < 60 && !/^(INT|EXT|INT\.\/EXT|INT\/EXT|I\/E)[\.\s\-]/i.test(trimmed) && !/[a-z]/.test(trimmed.replace(/\([^)]+\)/g, ""))) {
      const characterName = charMatch[1].trim();
      let hasFollowingDialogue = false;
      for (let j = i + 1; j < lines.length && j <= i + 3; j++) {
        if (lines[j].trim()) {
          hasFollowingDialogue = true;
          break;
        }
      }
      if (hasFollowingDialogue) {
        currentCharacter = characterName;
        elements.push({
          type: "character",
          text: trimmed,
          character: characterName
        });
        continue;
      }
    }
    if (/^\([^)]+\)$/.test(trimmed) && currentCharacter) {
      elements.push({
        type: "parenthetical",
        text: trimmed,
        character: currentCharacter
      });
      continue;
    }
    if (currentCharacter) {
      elements.push({
        type: "dialogue",
        text: trimmed,
        character: currentCharacter
      });
      continue;
    }
    elements.push({ type: "action", text: trimmed });
  }
  return elements;
}
function extractCharacters(elements) {
  const counts = /* @__PURE__ */ new Map();
  for (const el of elements) {
    if (el.type === "dialogue" && el.character) {
      counts.set(el.character, (counts.get(el.character) ?? 0) + 1);
    }
  }
  return counts;
}
function preprocessTextForSpeech(text, elementType) {
  let processed = text;
  if (elementType === "scene_heading") {
    processed = processed.replace(/\bINT\.?\b/gi, "inside");
    processed = processed.replace(/\bEXT\.?\b/gi, "outside");
    processed = processed.replace(/\bINT\.\/EXT\.?\b/gi, "inside and outside");
    processed = processed.replace(/\bI\/E\.?\b/gi, "inside and outside");
  }
  processed = processed.replace(/\.{3,}/g, "");
  processed = processed.replace(/\u2026/g, "");
  processed = processed.replace(/--+/g, "");
  processed = processed.replace(/\u2014/g, "");
  processed = processed.replace(/([,;:!?])\s*\./g, "$1");
  processed = processed.replace(/\s+/g, " ").trim();
  return processed;
}
function shouldSpeak(type) {
  return type === "dialogue" || type === "action" || type === "scene_heading" || type === "transition";
}
function getSpeaker(type, character) {
  if (type === "dialogue" && character) return character;
  return "Narrator";
}
function buildDistinctDefaults(characters, language) {
  const defaults = /* @__PURE__ */ new Map();
  if (characters.length === 0) return defaults;
  defaults.set("Narrator", {
    voiceName: "af_heart",
    pitch: 1,
    rate: 0.95,
    language
  });
  const nonNarrator = characters.filter((c) => c.name !== "Narrator");
  const count = nonNarrator.length;
  if (count === 0) return defaults;
  const voicePool = KOKORO_VOICES.slice(1);
  const pitchMin = 0.6;
  const pitchMax = 1.5;
  const slowRates = [0.75, 0.8, 0.85, 0.9, 0.95];
  const fastRates = [1.1, 1.15, 1.2, 1.25, 1.3, 1.4];
  nonNarrator.forEach((char, i) => {
    const voiceIndex = i % voicePool.length;
    const voiceName = voicePool[voiceIndex].id;
    const pitchStep = count > 1 ? (pitchMax - pitchMin) / (count - 1) : 0;
    const rawPitch = pitchMin + pitchStep * i;
    const pitch = Math.round(rawPitch * 10) / 10;
    const rate = i % 2 === 0 ? slowRates[Math.floor(i / 2) % slowRates.length] : fastRates[Math.floor(i / 2) % fastRates.length];
    defaults.set(char.name, {
      voiceName,
      pitch,
      rate,
      language
    });
  });
  return defaults;
}
function useTableRead(elements, language, voiceAssignments) {
  const [isPlaying, setIsPlaying] = reactExports.useState(false);
  const [currentIndex, setCurrentIndex] = reactExports.useState(-1);
  const [isInitializing, setIsInitializing] = reactExports.useState(true);
  const [characters, setCharacters] = reactExports.useState([]);
  const [defaultVoiceAssignments, setDefaultVoiceAssignments] = reactExports.useState({});
  const isPlayingRef = reactExports.useRef(false);
  const shouldContinueRef = reactExports.useRef(false);
  const blockStartTimeRef = reactExports.useRef(0);
  const loopIndexRef = reactExports.useRef(-1);
  const defaultVoicesRef = reactExports.useRef(/* @__PURE__ */ new Map());
  const voiceAssignmentsRef = reactExports.useRef(voiceAssignments);
  const elementsRef = reactExports.useRef(elements);
  const cancelCurrentRef = reactExports.useRef(null);
  const defaultsUsedKokoroRef = reactExports.useRef(false);
  reactExports.useEffect(() => {
    voiceAssignmentsRef.current = voiceAssignments;
  }, [voiceAssignments]);
  reactExports.useEffect(() => {
    elementsRef.current = elements;
  }, [elements]);
  reactExports.useEffect(() => {
    const counts = extractCharacters(elements);
    const sorted = Array.from(counts.entries()).sort((a, b) => b[1] - a[1]).map(([name, count]) => ({ name, count }));
    setCharacters([{ name: "Narrator", count: 0 }, ...sorted]);
  }, [elements]);
  reactExports.useEffect(() => {
    const checkReady = () => {
      const provider = ttsRegistry.getActiveProvider();
      if (!provider) {
        setIsInitializing(true);
        return;
      }
      setIsInitializing(!provider.isReady());
    };
    checkReady();
    ttsRegistry.addListener(checkReady);
    const interval = setInterval(checkReady, 500);
    setTimeout(() => clearInterval(interval), 1e4);
    return () => {
      ttsRegistry.removeListener(checkReady);
      clearInterval(interval);
    };
  }, []);
  reactExports.useEffect(() => {
    var _a;
    if (characters.length === 0) return;
    const kokoroProvider = ttsRegistry.getProvider("kokoro");
    const kokoroAvailable = (kokoroProvider == null ? void 0 : kokoroProvider.isAvailable()) ?? false;
    if (kokoroAvailable) {
      if (!defaultsUsedKokoroRef.current) {
        defaultsUsedKokoroRef.current = true;
        const map2 = buildDistinctDefaults(characters, language);
        defaultVoicesRef.current = map2;
        const asRecord2 = {};
        for (const [k, v] of map2) asRecord2[k] = v;
        setDefaultVoiceAssignments(asRecord2);
      }
      return;
    }
    if (defaultVoicesRef.current.size > 0) return;
    const provider = ttsRegistry.getActiveProvider();
    const voices = (provider == null ? void 0 : provider.getVoices()) ?? [];
    if (voices.length === 0) return;
    const map = /* @__PURE__ */ new Map();
    const count = characters.length;
    map.set("Narrator", {
      voiceName: (_a = voices[0]) == null ? void 0 : _a.id,
      pitch: 1,
      rate: 0.95,
      language
    });
    const nonNarrator = characters.filter((c) => c.name !== "Narrator");
    const voicePool = voices.slice(1);
    const pitchMin = 0.6;
    const pitchMax = 1.5;
    const slowRates = [0.75, 0.8, 0.85, 0.9, 0.95];
    const fastRates = [1.1, 1.15, 1.2, 1.25, 1.3];
    nonNarrator.forEach((char, i) => {
      var _a2;
      const voiceName = voicePool.length > 0 ? voicePool[i % voicePool.length].id : (_a2 = voices[0]) == null ? void 0 : _a2.id;
      const pitchStep = count > 1 ? (pitchMax - pitchMin) / (count - 1) : 0;
      const rawPitch = pitchMin + pitchStep * i;
      const pitch = Math.round(rawPitch * 10) / 10;
      const rate = i % 2 === 0 ? slowRates[Math.floor(i / 2) % slowRates.length] : fastRates[Math.floor(i / 2) % fastRates.length];
      map.set(char.name, { voiceName, pitch, rate, language });
    });
    defaultVoicesRef.current = map;
    const asRecord = {};
    for (const [k, v] of map) asRecord[k] = v;
    setDefaultVoiceAssignments(asRecord);
  }, [characters, language]);
  const buildSynthesisCall = reactExports.useCallback(
    (element) => {
      if (!shouldSpeak(element.type)) return null;
      const rawText = preprocessTextForSpeech(element.text, element.type);
      if (!rawText) return null;
      const speaker = getSpeaker(element.type, element.character);
      const assigned = voiceAssignmentsRef.current[speaker];
      const defaults = defaultVoicesRef.current.get(speaker);
      const va = assigned ?? defaults ?? { pitch: 1, rate: 1 };
      const settings = {
        voiceId: va.voiceName,
        pitch: va.pitch,
        rate: va.rate,
        language: va.language ?? language
      };
      return { text: rawText, settings };
    },
    [language]
  );
  const runPlayback = reactExports.useCallback(
    (startIndex) => {
      loopIndexRef.current = startIndex;
      const advance = async () => {
        if (!shouldContinueRef.current) return;
        const idx = loopIndexRef.current;
        const elems = elementsRef.current;
        if (idx >= elems.length) {
          shouldContinueRef.current = false;
          isPlayingRef.current = false;
          setIsPlaying(false);
          setCurrentIndex(-1);
          return;
        }
        const element = elems[idx];
        const call = buildSynthesisCall(element);
        setCurrentIndex(idx);
        blockStartTimeRef.current = Date.now();
        if (!call) {
          loopIndexRef.current = idx + 1;
          advance();
          return;
        }
        const provider = ttsRegistry.getActiveProvider();
        if (!provider) {
          loopIndexRef.current = idx + 1;
          advance();
          return;
        }
        let cancelled = false;
        cancelCurrentRef.current = () => {
          cancelled = true;
          provider.cancel();
        };
        try {
          await provider.synthesize(call.text, call.settings);
          cancelCurrentRef.current = null;
          if (cancelled || !shouldContinueRef.current) return;
          loopIndexRef.current = loopIndexRef.current + 1;
          advance();
        } catch {
          cancelCurrentRef.current = null;
          if (!shouldContinueRef.current) return;
          loopIndexRef.current = loopIndexRef.current + 1;
          advance();
        }
      };
      advance();
    },
    [buildSynthesisCall]
  );
  const play = reactExports.useCallback(() => {
    var _a, _b;
    if (isInitializing) return;
    (_a = cancelCurrentRef.current) == null ? void 0 : _a.call(cancelCurrentRef);
    (_b = ttsRegistry.getActiveProvider()) == null ? void 0 : _b.cancel();
    shouldContinueRef.current = true;
    isPlayingRef.current = true;
    setIsPlaying(true);
    const start = currentIndex <= 0 || currentIndex >= elementsRef.current.length ? 0 : currentIndex;
    loopIndexRef.current = start;
    setCurrentIndex(start);
    blockStartTimeRef.current = Date.now();
    setTimeout(() => {
      if (shouldContinueRef.current) runPlayback(start);
    }, 50);
  }, [isInitializing, currentIndex, runPlayback]);
  const pause = reactExports.useCallback(() => {
    var _a, _b;
    shouldContinueRef.current = false;
    isPlayingRef.current = false;
    (_a = cancelCurrentRef.current) == null ? void 0 : _a.call(cancelCurrentRef);
    (_b = ttsRegistry.getActiveProvider()) == null ? void 0 : _b.cancel();
    setIsPlaying(false);
  }, []);
  const skipForward = reactExports.useCallback(() => {
    var _a, _b;
    const next = loopIndexRef.current + 1;
    if (next >= elementsRef.current.length) return;
    const wasPlaying = isPlayingRef.current && shouldContinueRef.current;
    shouldContinueRef.current = false;
    (_a = cancelCurrentRef.current) == null ? void 0 : _a.call(cancelCurrentRef);
    (_b = ttsRegistry.getActiveProvider()) == null ? void 0 : _b.cancel();
    cancelCurrentRef.current = null;
    loopIndexRef.current = next;
    setCurrentIndex(next);
    blockStartTimeRef.current = Date.now();
    if (wasPlaying) {
      shouldContinueRef.current = true;
      isPlayingRef.current = true;
      setIsPlaying(true);
      setTimeout(() => {
        if (shouldContinueRef.current) runPlayback(next);
      }, 50);
    }
  }, [runPlayback]);
  const skipBack = reactExports.useCallback(() => {
    var _a, _b;
    const elapsed = Date.now() - blockStartTimeRef.current;
    const idx = loopIndexRef.current;
    const target = elapsed < 2e3 && idx > 0 ? idx - 1 : Math.max(0, idx);
    const wasPlaying = isPlayingRef.current && shouldContinueRef.current;
    shouldContinueRef.current = false;
    (_a = cancelCurrentRef.current) == null ? void 0 : _a.call(cancelCurrentRef);
    (_b = ttsRegistry.getActiveProvider()) == null ? void 0 : _b.cancel();
    cancelCurrentRef.current = null;
    loopIndexRef.current = target;
    setCurrentIndex(target);
    blockStartTimeRef.current = Date.now();
    if (wasPlaying) {
      shouldContinueRef.current = true;
      isPlayingRef.current = true;
      setIsPlaying(true);
      setTimeout(() => {
        if (shouldContinueRef.current) runPlayback(target);
      }, 50);
    }
  }, [runPlayback]);
  const seekTo = reactExports.useCallback(
    (index) => {
      var _a, _b;
      if (index < 0 || index >= elementsRef.current.length) return;
      shouldContinueRef.current = false;
      (_a = cancelCurrentRef.current) == null ? void 0 : _a.call(cancelCurrentRef);
      (_b = ttsRegistry.getActiveProvider()) == null ? void 0 : _b.cancel();
      cancelCurrentRef.current = null;
      loopIndexRef.current = index;
      setCurrentIndex(index);
      blockStartTimeRef.current = Date.now();
      shouldContinueRef.current = true;
      isPlayingRef.current = true;
      setIsPlaying(true);
      setTimeout(() => {
        if (shouldContinueRef.current) runPlayback(index);
      }, 50);
    },
    [runPlayback]
  );
  const progress = elements.length > 0 ? (Math.max(0, currentIndex) + 1) / elements.length * 100 : 0;
  const availableVoices = typeof window !== "undefined" && window.speechSynthesis ? window.speechSynthesis.getVoices() : [];
  return {
    isPlaying,
    currentIndex,
    progress,
    play,
    pause,
    skipForward,
    skipBack,
    seekTo,
    isInitializing,
    characters,
    availableVoices,
    defaultVoiceAssignments
  };
}
function ScreenplayPlayer({
  screenplayId,
  onBack
}) {
  const { data: screenplay, isLoading } = useGetScreenplay(screenplayId);
  const { data: savedSettings = [] } = useGetVoiceSettings(screenplayId);
  const [parsedElements, setParsedElements] = reactExports.useState([]);
  const [voiceAssignments, setVoiceAssignments] = reactExports.useState({});
  const [activeTab, setActiveTab] = reactExports.useState("play");
  const scrollViewportRef = reactExports.useRef(null);
  const elementRefs = reactExports.useRef(/* @__PURE__ */ new Map());
  const {
    isPlaying,
    currentIndex,
    progress,
    play,
    pause,
    skipForward,
    skipBack,
    seekTo,
    isInitializing,
    characters,
    availableVoices,
    defaultVoiceAssignments
  } = useTableRead(
    parsedElements,
    (screenplay == null ? void 0 : screenplay.language) ?? "en-US",
    voiceAssignments
  );
  const handleVoiceAssignmentsChange = reactExports.useCallback(
    (assignments) => {
      setVoiceAssignments(assignments);
    },
    []
  );
  reactExports.useEffect(() => {
    if (screenplay == null ? void 0 : screenplay.content) {
      setParsedElements(parseScreenplay(screenplay.content));
    }
  }, [screenplay]);
  const scrollToBlock = reactExports.useCallback((index) => {
    const viewport = scrollViewportRef.current;
    const el = elementRefs.current.get(index);
    if (!viewport || !el) return;
    const viewportRect = viewport.getBoundingClientRect();
    const elRect = el.getBoundingClientRect();
    const offset = el.offsetTop - viewport.scrollTop - viewportRect.height / 2 + elRect.height / 2;
    viewport.scrollBy({ top: offset, behavior: "smooth" });
  }, []);
  reactExports.useEffect(() => {
    if (currentIndex < 0) return;
    scrollToBlock(currentIndex);
  }, [currentIndex, scrollToBlock]);
  reactExports.useEffect(() => {
    if (activeTab !== "play") return;
    if (currentIndex >= 0) {
      const t = setTimeout(() => scrollToBlock(currentIndex), 80);
      return () => clearTimeout(t);
    }
  }, [activeTab, currentIndex, scrollToBlock]);
  const characterColorMap = /* @__PURE__ */ new Map();
  characters.forEach((char, i) => {
    characterColorMap.set(char.name, i);
  });
  const sceneHeadings = reactExports.useMemo(
    () => parsedElements.map((el, index) => ({ el, index })).filter(({ el }) => el.type === "scene_heading"),
    [parsedElements]
  );
  const currentSceneIndex = reactExports.useMemo(() => {
    if (currentIndex < 0 || sceneHeadings.length === 0) return "";
    let last = sceneHeadings[0].index;
    for (const { index } of sceneHeadings) {
      if (index <= currentIndex) last = index;
      else break;
    }
    return String(last);
  }, [currentIndex, sceneHeadings]);
  const handleProgressClick = reactExports.useCallback(
    (e) => {
      if (parsedElements.length === 0) return;
      const rect = e.currentTarget.getBoundingClientRect();
      const ratio = Math.max(
        0,
        Math.min(1, (e.clientX - rect.left) / rect.width)
      );
      const targetIndex = Math.round(ratio * (parsedElements.length - 1));
      seekTo(targetIndex);
    },
    [parsedElements.length, seekTo]
  );
  if (isLoading) {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "container mx-auto px-4 py-8 max-w-5xl", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-8 w-24 mb-6" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-12 w-72 mb-4" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { className: "h-96 w-full" })
    ] });
  }
  if (!screenplay) {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "container mx-auto px-4 py-8 max-w-5xl", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Button, { variant: "ghost", onClick: onBack, className: "mb-4", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(ArrowLeft, { className: "w-4 h-4 mr-2" }),
        "Back"
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Card, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(CardContent, { className: "py-12 text-center", children: /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground", children: "Screenplay not found." }) }) })
    ] });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "container mx-auto px-4 pt-6 pb-44 max-w-5xl", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3 mb-6", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        Button,
        {
          variant: "ghost",
          size: "sm",
          onClick: onBack,
          "data-ocid": "back-button",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(ArrowLeft, { className: "w-4 h-4 mr-1" }),
            "Back"
          ]
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-xl font-bold font-display truncate min-w-0", children: screenplay.title })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(Tabs, { value: activeTab, onValueChange: setActiveTab, className: "w-full", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(TabsList, { className: "grid w-full grid-cols-2 mb-4", "data-ocid": "tab-list", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(TabsTrigger, { value: "play", "data-ocid": "tab-play", children: "Play" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(TabsTrigger, { value: "voices", "data-ocid": "tab-voices", children: "Voice Studio" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(TabsContent, { value: "play", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Card, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(CardContent, { className: "p-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "div",
        {
          ref: scrollViewportRef,
          className: "overflow-y-auto h-[calc(100vh-360px)] min-h-[400px] scrollbar-thin",
          children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "screenplay-content p-6 space-y-1", children: [
            parsedElements.map((element, index) => {
              const isActive = index === currentIndex;
              const colorIdx = element.character ? getCharacterColor(
                characterColorMap.get(element.character) ?? 0
              ) : null;
              const color = colorIdx !== null ? CHARACTER_COLORS[colorIdx] : null;
              return /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "div",
                {
                  ref: (el) => {
                    if (el) elementRefs.current.set(index, el);
                    else elementRefs.current.delete(index);
                  },
                  role: "presentation",
                  onClick: () => seekTo(index),
                  onKeyUp: (e) => {
                    if (e.key === "Enter") seekTo(index);
                  },
                  className: `relative transition-all duration-200 rounded-sm px-3 py-1 cursor-pointer ${isActive ? "script-block-active" : "opacity-70 hover:opacity-90 hover:bg-muted/20"}`,
                  "data-ocid": isActive ? "active-block" : void 0,
                  children: [
                    element.type === "scene_heading" && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "font-bold uppercase text-xs tracking-widest text-primary py-2", children: element.text }),
                    element.type === "action" && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-foreground/90", children: element.text }),
                    element.type === "character" && color && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 mt-3 ml-[25%]", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                        "span",
                        {
                          className: `character-badge w-7 h-7 text-[10px] ${color.bg} ${color.text}`,
                          "aria-hidden": true,
                          children: getInitials(element.text)
                        }
                      ),
                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                        "p",
                        {
                          className: `font-bold uppercase text-sm tracking-wide ${color.text}`,
                          children: element.text
                        }
                      )
                    ] }),
                    element.type === "dialogue" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "p",
                      {
                        className: `ml-[25%] mr-[15%] ${isActive && color ? color.text : "text-foreground/80"}`,
                        children: element.text
                      }
                    ),
                    element.type === "parenthetical" && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "ml-[30%] mr-[20%] text-muted-foreground italic text-sm", children: element.text }),
                    element.type === "transition" && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-right text-xs uppercase tracking-widest text-muted-foreground py-2", children: element.text })
                  ]
                },
                `${element.type}-${index}`
              );
            }),
            parsedElements.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex flex-col items-center justify-center py-24 text-center", children: /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground", children: "No readable content found in this screenplay." }) })
          ] })
        }
      ) }) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(TabsContent, { value: "voices", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        VoiceStudio,
        {
          screenplayId,
          characters,
          availableVoices,
          voiceAssignments,
          defaultVoiceAssignments,
          onVoiceAssignmentsChange: handleVoiceAssignmentsChange,
          savedSettings,
          scriptLanguage: screenplay.language
        }
      ) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: "sticky-controls fixed bottom-0 left-0 right-0",
        "data-ocid": "playback-controls",
        children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "container mx-auto max-w-5xl px-4 py-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 mb-3", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs text-muted-foreground tabular-nums w-16 text-right flex-shrink-0", children: currentIndex >= 0 ? `${currentIndex + 1} / ${parsedElements.length}` : `0 / ${parsedElements.length}` }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "div",
              {
                className: "progress-track flex-1 cursor-pointer group relative",
                onClick: handleProgressClick,
                onKeyDown: (e) => {
                  if (e.key === "ArrowRight") skipForward();
                  if (e.key === "ArrowLeft") skipBack();
                },
                role: "slider",
                tabIndex: 0,
                "aria-label": "Playback position",
                "aria-valuemin": 0,
                "aria-valuemax": parsedElements.length,
                "aria-valuenow": Math.max(0, currentIndex),
                "data-ocid": "progress-bar",
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "div",
                    {
                      className: "progress-fill group-hover:opacity-90 transition-opacity",
                      style: { width: `${progress}%` }
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "div",
                    {
                      className: "absolute top-1/2 -translate-y-1/2 w-3 h-3 rounded-full bg-primary opacity-0 group-hover:opacity-100 transition-opacity shadow-md pointer-events-none",
                      style: { left: `calc(${progress}% - 6px)` }
                    }
                  )
                ]
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between gap-2 flex-wrap", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex-1 min-w-0 max-w-xs", children: sceneHeadings.length > 0 ? /* @__PURE__ */ jsxRuntimeExports.jsxs(
              Select,
              {
                value: currentSceneIndex,
                onValueChange: (v) => seekTo(Number(v)),
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    SelectTrigger,
                    {
                      className: "h-8 text-xs w-full",
                      "data-ocid": "scene-jump-select",
                      children: /* @__PURE__ */ jsxRuntimeExports.jsx(SelectValue, { placeholder: "Jump to scene…" })
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(SelectContent, { className: "max-h-64", children: sceneHeadings.map(({ el, index }) => /* @__PURE__ */ jsxRuntimeExports.jsx(
                    SelectItem,
                    {
                      value: String(index),
                      className: "text-xs",
                      children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "truncate block max-w-[260px]", children: el.text.length > 50 ? `${el.text.slice(0, 50)}…` : el.text })
                    },
                    index
                  )) })
                ]
              }
            ) : /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs text-muted-foreground", children: "No scenes" }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3 flex-shrink-0", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Button,
                {
                  variant: "outline",
                  size: "icon",
                  onClick: skipBack,
                  disabled: currentIndex < 0 || isInitializing,
                  "aria-label": "Skip back",
                  "data-ocid": "skip-back",
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(SkipBack, { className: "w-4 h-4" })
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Button,
                {
                  size: "lg",
                  onClick: isPlaying ? pause : play,
                  disabled: isInitializing || parsedElements.length === 0,
                  className: "w-32 gap-2",
                  "data-ocid": "play-pause",
                  children: isInitializing ? /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "w-5 h-5 animate-spin" }) : isPlaying ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Pause, { className: "w-5 h-5" }),
                    "Pause"
                  ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Play, { className: "w-5 h-5" }),
                    "Play"
                  ] })
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Button,
                {
                  variant: "outline",
                  size: "icon",
                  onClick: skipForward,
                  disabled: currentIndex >= parsedElements.length - 1 || isInitializing,
                  "aria-label": "Skip forward",
                  "data-ocid": "skip-forward",
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(SkipForward, { className: "w-4 h-4" })
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex-1 max-w-xs hidden sm:block" })
          ] })
        ] })
      }
    )
  ] });
}
export {
  ScreenplayPlayer
};
